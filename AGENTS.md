# Campus Loop 项目约定

- 本仓库管理三端与协作文档。先核对相关源码、配置及未提交改动，只加载当前任务需要的上下文；保护他人改动。
- 保留管理端和 UniApp 各自 lockfile 与构建器；变更依赖须解释原因并构建。公共脚本入口见 README。
- 本地环境统一通过 scripts/run.mjs 读取根 .env；凭据、上传与数据不进 Git、不进文档或日志。数据库写入仅允许明确的新项目开发或隔离测试库，不访问参考工程数据。
- API 以 docs/api-contract.md 为准。跨端字段、状态、分页或错误码先同步契约；不能用假成功响应替代未实现功能。
- 匹配推荐无副作用；交换创建必须重新校验，事务、锁、幂等和占用规则见 docs/architecture.md。改变规则需补关键边界测试。
- 新界面使用 shared/design-tokens.css 与公共控件层。所有交互控件覆盖材质、状态、连续动效、焦点及降级；内容表面保持稳定可读。禁止重新引入参考工程的角色品牌。规范见 docs/design-system.md。
- 使用 main + feature/* + fix/*；至少一名其他成员审核且 CI 通过后合并。职责分工不限制目录访问，共享文件先协商。详见 CONTRIBUTING.md。
- 完成已授权实现与相关验证后交付；分别报告构建、测试、浏览器、真实数据库及微信平台的证据，不把一种结果替代另一种。
- 长期规则保持简明；架构、状态、设计及任务分别维护在 docs/ 下，不依赖个人绝对路径、私人技能或全局配置。
