# Campus Loop 管理端控件覆盖

2026-09-09 布局增量：侧栏独立滚动、图标不压缩、图文按钮间距、手机表格内部滚动、单层弹窗和正常排布插画。当前验证见[前端修复记录](../../docs/frontend-refinement.md)；下文历史记录保留原验收范围。

入口：`/controls`。主题语义来自仓库 `shared/design-tokens.css`，Element Plus 映射与页面布局在 `src/styles/base.css`，统一材质和动效在 `src/styles/liquid-glass.css`。不依赖私人技能或绝对本机路径。

| 控件 | 公共实现/验证入口 |
| --- | --- |
| 导航、侧栏、账户入口 | App.vue；选中底板连续移动、手机收起与 Escape、键盘焦点 |
| Tab、分段、单/复选 | Element Plus + 统一 indicator/selected 状态；实验室 01/04 |
| 按钮、图标按钮 | 统一轻边缘与按压；实验室 02；禁用/加载不会提交业务请求 |
| 搜索、输入、多行、错误 | 稳定输入底色，焦点与错误边缘；实验室 03、登录表单 |
| 选择器、日期、开关 | 单层 Popper、方向感知展开、原生键盘支持；实验室 04 |
| 分页、上传 | 统一按压、焦点与禁用；实验室 06。上传会真实调用接口 |
| 下拉、提示、气泡 | 单层玻璃外壳与触发点方向；实验室 05 |
| 消息、通知、确认框 | 单层小面积材质，关闭与自动消失；实验室 02/05 |
| 弹窗、抽屉 | SysDialog / Element Drawer + useOverlayLock；实验室 05、物品详情 |
| 商品、表格、长表单 | 固定内容底色，内部操作沿用控件规范；不对每张卡片实时模糊 |
| 物品审核表单 | Items / ReviewDecisionDialog；开发环境 `/fixtures/item-review` 覆盖理由错误、loading/disabled、403、409回读和失败保留。生产不注册夹具路由，写接口未合入前正式按钮禁用 |
| 分类维护表单 | Categories / CategoryFormDialog；开发环境 `/fixtures/categories` 提供真实读取、空白/长度校验、提交防重入、错误保留与409回读夹具。生产不注册夹具路由，写接口未合入前正式按钮禁用 |
| 账号查询与启停 | Users / UserStatusDialog；真实服务端筛选分页、审计抽屉、1–500字理由、loading防重入、403保留和409回读；确认弹层说明会话撤销及恢复登录影响 |
| 交换争议追溯 | Disputes / ExchangeTrace；生产入口明确 ADMIN API 待开发，开发环境 `/fixtures/disputes` 覆盖双方/三方流向、长理由、事件、加载/错误/空态和响应式抽屉；不提供未授权裁决按钮 |
| 举报受理与处理 | Reports / ReportActionDialog；生产入口明确后端待开发，开发环境 `/fixtures/reports` 覆盖筛选/分页、目标与证据状态、理由校验、loading 防重入、403 与 409 回读保留。夹具不写本地数组、不冒充业务成功 |
| 业务统计卡片与说明 | Overview / BusinessStatsPanel；稳定内容底色、文字口径、真实0/不可用/失败状态及待审记录入口。开发环境 `/fixtures/stats` 使用固定虚构数据覆盖0、超限和部分失败，不调用 API |

| 交换管理读取 | Exchanges；状态选择、分页、刷新、详情抽屉、参与者/流向/安全创建快照与处理时间线；仅调用 ADMIN 读取接口，无代确认/代交接操作。列表与详情分别处理加载、空结果、403/404/409、重试、乱序响应及账号切换；抽屉沿用 Element Plus 焦点/Escape 和 useOverlayLock |

所有控件共享悬停、聚焦、按下、选中、加载、禁用、错误、打开/关闭的相应状态。表格本身保持阅读效率。原生按钮和 Element Plus 保留键盘语义与焦点环，切换主题保持明确语义色；深色填充主按钮使用深色文字以保持对比。

本阶段新增页面继续复用上述入口：分类维护、物品审核、账号启停的筛选/分页/表单/抽屉，以及 D-03 推荐预览的跨端公共行为均不另造材质。实验室新增“失败反馈”明确为本地视觉演示，不发送业务请求；共享焦点与遮罩值来自根 token。

动画采用可逆 CSS transition。浮层根据 `data-popper-placement` 选择展开边，离场遮罩设为不接收指针，防止快速重新打开时吞点击。弹窗和抽屉必须使用公共锁服务并关闭 Element Plus 的单实例 `lock-scroll`；不得另行强制清除 body 的其他锁。

降低开销：只对导航/小浮层使用 blur；设备核心数低、低内存或节省流量时自动 `data-glass="reduced"`，也可在实验室手动简化。无滤镜支持、减少透明度或高对比偏好时使用稳定底色。系统 `prefers-reduced-motion` 始终生效；实验室「减少动态效果」通过 `html.cl-motion-reduced` 使用等效规则并保存本地偏好。两者都将动画/过渡时长降为 0，移除明显按压/悬停位移。

这是 Web 的材质近似，不是 Apple 原生光学折射。微信原生控件规范见用户端文档。

## 本轮浏览器检查

2026-09-07，Codex 内置浏览器：

- 桌面 1280×720、手机 390×844、平板 820×1180 检查；手机和平板 document scrollWidth 等于 viewport，无横向页面溢出。
- Tab/分段移动、必填错误消除、分类选择、日期选择、分页、菜单、消息与通知均实际操作；登录页空提交显示真实必填错误。
- 弹窗和抽屉各连续开关 4 次、混合 Escape→重开→取消→抽屉→关闭→重开；修复后最终无可见遮罩、html/body 无遗留锁，overflow 恢复 visible。
- 深色实际切换；修复 Element Plus 暗色默认变量优先级后，主色仍为 Campus Loop 粉色，内容底色遵循共享 token。
- 简化材质开关实际检查 topbar `backdrop-filter: none`。
- 减少动态效果开关实际检查 dialog、drawer、导航与分段指示器的 computed transitionDuration 均为 `0s`；刷新后偏好仍生效、关闭后无残留遮罩。
- 系统减少动态效果 CSS 分支已检查；未修改用户操作系统的辅助功能设置。自动化/构建、浏览器 UI 和真实数据库链路应分别记录，不相互替代。

2026-09-08 的 C-02 增量覆盖见根 `docs/verification.md`；本文件保留映射规则，不用旧浏览器记录冒充本次验收。

## A-02 物品审核增量

管理端增加状态选择、真实审核理由与1000字校验、提交锁定、409保留输入和显式回读、分页审计及快照展开；使用原有 Element Plus / SysDialog、共享焦点/减少动态效果样式。用户端本人审核进度使用 LoopPicker、LoopButton 与既有材质层，覆盖加载、空结果、错误重试、分页禁用及当前会话切换后丢弃旧响应。浏览器和平台验证范围见 [审核交付说明](../../docs/a02-item-review.md)，不将构建视为微信真机验收。

## C-03 交换管理增量

`/exchanges` 复用既有 Element Plus 选择器、按钮、表格、分页、抽屉、骨架屏、提示和时间线；内容使用稳定底色，继承全局深浅主题、焦点与减少动态效果。抽屉关闭立即清理详情请求和公共页面锁；列表分页在加载期间保留范围，记录总数缩小时回读最后有效页。

2026-09-08 本轮管理端自动化 31 项、生产构建通过。独立浏览器组件检查使用明确虚构的 API 响应，实际操作了分页、状态选择、详情打开/刷新、Enter/Escape、409 重试和无历史快照/事件；1280×900 桌面及375/768宽度抽屉在减少动态效果下无页面/抽屉横向溢出，关闭后无公共锁残留，页面 JavaScript 错误为0。该夹具检查只证明页面交互与布局，不是实际管理 API、真实数据库或阶段二业务全链路验收；实际服务证据由本次综合验收单独记录。


## 2026-09-09 整合控件覆盖

分类维护、举报处理、履历核验、本人物品编辑/下架、举报提交与交换动作复用 SysDialog / LoopSheet、公共按钮、输入与选择器。LoopInput 在 H5 使用原生输入并在微信保留 input 能力；覆盖 disabled、密码、confirm 与输入清理。错误、加载、版本冲突和结果未知需真实回读，私有证据通过鉴权内容接口读取。浏览器与微信验证边界见全项目审查报告。


### 平台补全控件（2026-09-09）

权限分配的单选/复选/文本框与对话框、争议处理表单、运营区间选择/分页/数据表，以及用户端资料上传、站内消息按钮、重新上架与履历处理记录沿用现有公共组件和主题变量。新增表单包含加载、禁用、失败与版本冲突恢复状态；不增加独立动效引擎。


### 发布与校园动态（2026-09-09）

账号目录标签、编辑/新增/重置密码弹窗和操作时间线继承 Element Plus 共享样式；动态处理抽屉使用现有页面锁和加载/禁用/错误状态。用户端复用 LoopSegment、LoopInput、LoopPicker、LoopButton、LoopLayout：发布上传显示进度与持久成功提示，完成页给出后续入口；动态编辑器、互动与分页沿用相同焦点、深色与减少动态效果。实际浏览器已验证本机真实 API 的发布、上传、匹配、动态、回复、资料保存及举报处理；390px 用户端讨论页宽度 390、内容 358，无横向溢出。微信为构建验收，未进行真机操作。

### 同学试用反馈修复（2026-09-09）

账号管理操作列使用固定宽度与可换行的独立按钮，保留 Element Plus 的悬停、焦点、禁用和深色样式。路由内容增加进入/离开过渡，并在减少动态效果时关闭。
