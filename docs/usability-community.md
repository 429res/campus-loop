# 发布、匹配与校园动态改进

本轮从新用户完整使用流程验收，延续已部署的站内业务；参考 ProjectStudy-self 管理端的账号目录、角色分类、资料维护、明确操作反馈，以及 Misskey 的动态、回复与表态交互。

## 契约与数据影响

- V17 为发布物品关联一条自动需求，补齐已有物品，发布/修改求换内容与关联需求同事务保存。只有审核通过、未占用的物品参与原双方/三方匹配；原独立需求继续可用，不自动创建交换。自动需求由物品编辑维护，与手动需求区分。
- 匹配入口增加本人参与条件、审核状态与下一步操作；发布后给出明确结果和匹配入口，上传显示预览、进度、成功/失败和替换/移除。
- 管理端区分普通用户和管理员，提供创建、资料编辑、启停、权限分配、密码重置与操作记录。管理员的访问权及密码仅由最高权限管理员管理；保护当前账号与最后一名最高权限管理员。密码不回显、不进入审计。
- V18 新增站内动态、回复、点赞及举报记录。动态可附本人可见物品；纯文本渲染，作者可撤回，COMMUNITY 权限可处理举报与隐藏/恢复。动态与回复按时间分页，用户身份由服务端确定，点赞幂等，回复通知原作者。
- 登录成功替换登录历史；已登录用户返回登录页会恢复目的页面；增加站内返回入口。只在实际认证失败时清理当前会话，网络失败不冒充登出。

## 验收边界

以两位新用户分别发布互补物品为起点，检查审核前提示、审核后自动匹配、交换发起；另验收信息流发布/回复/点赞/举报与管理员处置，以及账号管理、上传反馈和浏览器返回。数据库及并发检查在隔离 MySQL 执行，云端仅用专门虚构记录验证。本轮不启用自动审核、OSS 或其他付费外部服务，不引入 Misskey 服务或联邦网络。

参考：<https://misskey-hub.net/en/docs/for-users/features/timeline/>、<https://misskey-hub.net/en/>。


## 本轮验证记录

- 管理端 42 项、用户端 107 项、Java 核心 78 项单元检查通过；后端接口 194 项，H2 跳过 26 项 MySQL 专项，隔离 MySQL 8.4.11 全部通过、零跳过。
- 本机独立 MySQL 从 V16 升至 V18 成功。两位新用户互补物品：浏览器上传进度/上传成功、发布结果页、审核前说明、后台人工通过、自动出现双方方案；未另建手动需求。后台资料编辑/回读和操作记录、普通/管理员目录、双方账号权限边界通过。
- 实际动态发布、关联本人公开物品、点赞和回复通过；另一位虚构用户回复及举报通过 API 写入，后台可查看并隐藏/恢复，隐藏后匿名详情返回 404，处理原因与记录可回读。390px 讨论页面无横向溢出。
- 用户端浏览器返回保留会话；用户端与管理端直接再次进入登录地址均自动回到已登录页面。
- 微信构建与实际开发者工具/真机验证分开报告；本轮不宣称已经真机验证。

GitHub 以分支与 [PR #57](https://github.com/429res/campus-loop/pull/57) 交付，基于尚待成员审核的 [PR #56](https://github.com/429res/campus-loop/pull/56)，不直接合入 main。应用版本 `defca54` 已于 2026-09-09 发布到阿里云服务器，两端 HTTPS 公网可用；[发布包](https://github.com/429res/campus-loop/releases/tag/classmate-preview-defca54)。

## 公网发布验收（2026-09-09）

- 新版应用镜像 `defca54` 正常运行，V17/V18 迁移成功；发布前数据库及上传文件备份完成，定时备份任务保持运行。切换后的首次公网检查遇到一次 522，随后服务器、浏览器及本机重复请求正常，完整业务验收通过。
- 使用两名独立虚构用户发布互补物品：自动需求及其写保护、待审核提示、人工审核后双方推荐均通过，没有额外创建手动需求，也没有创建真实交换。
- 动态发布重试幂等、重复点赞、回复、举报、后台隐藏/恢复、匿名不可见、处置审计通过。
- 最高权限账号创建受限管理员成功；受限管理员可维护普通用户资料，不能修改管理员；最高权限重置虚构用户密码后旧会话失效，新密码可登录，操作可审计。
- 公网 Chrome 已显示旧物品回填后的真实交换推荐；后台普通用户与管理员目录、校园动态页面可用。验收结束后撤回虚构动态、下架物品并停用三名临时账号，清理无错误。
- 应用提交 `defca54` 的 GitHub 六项 CI 全部通过（管理端、H5、微信、后端、MySQL、Windows）。本地最终微信产物检查 6 项通过；未执行微信真机验证。后续文档提交不改变已部署应用。
