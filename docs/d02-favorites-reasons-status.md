# D-02 收藏夹与推荐理由展示

更新日期：2026-09-08。本切片接入已合入 main 的 B-02 独立需求推荐；收藏依赖尚未就绪，因此只提供明确待开发入口，不调用草案接口、不使用本地缓存伪造持久化。

## 实际接入

- 推荐页调用登录接口 `GET /api/matches/independent?ruleVersion=independent-v2`，不回退或混入公开 `legacy-v1` 结果。
- 页面直接展示服务端返回的双方/三方参与者、提供者→接收者物品流向、`demandId`、其他命中需求数量、命中分类、命中标签、reason、explanation 与规则版本；不在前端重算得分或选择需求。
- 分类明确标为硬条件，标签明确标为排序依据；requiredTags 和最低成色不显示为已启用规则。
- 刷新会取消旧请求并以 generation 防止旧响应覆盖。401 要求重新登录；422 单独解释为整次结果超限，不显示成空推荐；空数组单独显示真实空态。
- 页面只执行 GET；交换按钮禁用并标记留到 D-03，推荐浏览和刷新不创建交换、占用或其他业务状态。

## 收藏依赖反馈

A-02 第一批 PR #16 只提供本人列表、编辑与下架。`docs/api-contract.md` 中收藏路径仍位于“后续接口设计”，代码库没有收藏控制器、迁移或测试，未确认分页字段、不可见物品返回规则及幂等响应。因此本切片不调用 `PUT/DELETE /items/{id}/favorite`，不在 ItemCard 注入虚假收藏状态，也不使用 localStorage 宣称跨会话一致。

个人页新增“我的收藏”入口，详情展示禁用的“收藏·接口待开发”，收藏页明确说明依赖边界。待 A 补齐唯一约束、本人分页、幂等添加/取消和下架可见性契约后，再在独立切片开放写操作与真实列表。

## 验证记录

- 构建：在 `wx-project-self` 运行 `npm run build:h5` 与 `npm run build:mp-weixin`，均通过；`npm run test:mp-build` 的 6 项微信生成物接线检查通过。
- 自动化：`node scripts/run.mjs test` 通过用户端 29 项（含 3 项推荐旧响应/账号切换保护）、匹配纯规则 42 项和 H2 API 集成 45 项；独立推荐用例覆盖真实两方/三方方向、命中需求、规则版本、空结果、422 边界及读取前后数据库无写入。`node scripts/repository-check.mjs` 通过。
- 真实数据库：`node scripts/mysql-test.mjs` 在一次性隔离 MySQL 8.4 容器应用 V1–V4 后通过同一组 42+45 项后端测试；容器完成后移除。收藏接口未实现，因此没有收藏写入、跨会话或幂等数据库结果可验证。
- H5 浏览器：实际启动本地 H5，检查推荐未登录态、规则说明和收藏待开发页；737px 视口无横向溢出。没有安全可用的测试登录会话，故未在浏览器展示真实两方/三方数据，也未完整运行深浅色、键盘焦点、快速弹层、减少动态和滤镜降级。
- 微信：小程序编译及生成物接线检查通过；微信开发者工具和真机均未运行。
