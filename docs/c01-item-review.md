> A-02 第四批更新：本 PR 已接通真实审核接口、状态筛选、详情、审计及409保留理由后显式回读。旧接口草案不再作为接入依据，当前字段/规则/验证见 [a02-item-review.md](a02-item-review.md) 与 [API契约](api-contract.md)。以下保留 C 初始只读切片的历史验收范围。

# C-01 物品审核接入说明

状态：管理端只读切片与审核表单组件夹具已实现；A-02 审核 API、审核迁移和跨端发布策略未合入，审核闭环未完成。

## 当前事实

- `GET /api/admin/items` 仅支持 `page/size/keyword/categoryId`；A-02 本人物品切片已为共用 `ItemView` 增加 `version`，但审核人、审核时间、审核理由及管理员审核详情接口仍未提供。
- `POST /api/admin/reviews/{id}/decision` 仍是契约占位，没有 controller/service 实现。
- 发布服务仍直接写 `AVAILABLE`。V1 表约束预留 `DRAFT/PENDING_REVIEW/AVAILABLE/RESERVED/EXCHANGED/HIDDEN`，但预留枚举不等于已确定审核状态机。
- 当前匹配及 B-02 分支都只选择 `AVAILABLE`；D-01 当前发布和展示仍按直发 `AVAILABLE` 运行。

因此管理端把现有 `AVAILABLE` 标注为“历史直发”，不把它解释成已审核，也不在前端迁移状态。正式通过/驳回和状态筛选保持禁用。

## A-02 最小依赖

1. 管理列表增加可选 `status` 筛选，沿用 A-02 已提供的非负整数 `ItemView.version`；提供能读取非公开物品及最新审核元数据的 ADMIN 详情。
2. 审核决定只允许 ADMIN；请求至少包含服务端约定的决定、非空理由和当前 `version`。成功返回持久化后的物品与审核信息，随后前端回读列表和详情。
3. 服务端锁定物品并校验 `PENDING_REVIEW + version`。旧版本、已被处理或其他不允许状态返回409且不写入；普通用户返回403。
4. 审核记录至少提供处理人显示名、UTC处理时间、理由及决定；保留审计记录，不覆盖历史。
5. A 与 D 同步新发布何时从 `AVAILABLE` 改为 `PENDING_REVIEW`，以及旧 `AVAILABLE` 数据的一次性迁移或保留策略。迁移只能由后端/数据库明确完成。

接口路径、决定枚举和理由长度仍须 A/C 确认。组件夹具暂用 `APPROVE/REJECT` 和 `{decision,reason,version}` 检查交互，不代表已经可调用的正式契约。

## B-02 与 D-01 影响

- B：推荐继续以服务端 `AVAILABLE` 为唯一可匹配状态；`PENDING_REVIEW`、驳回后的状态及其他状态不得进入候选。管理端不会修改或复制推荐规则。
- D：发布默认状态切换必须与 A 的后端提交同批协调；用户端需要明确展示待审/驳回结果，不能把提交成功描述为已上架。具体状态字段和理由可见性确认前，本 PR 不改用户端。

## 已交付界面

- 复用管理物品分页、关键词/分类查询和详情抽屉；增加审核状态提示、历史 `AVAILABLE` 说明及预留审核元数据展示。
- 开发环境 `/fixtures/item-review` 提供通过/驳回表单夹具，覆盖理由必填、版本请求、防重复提交、403、409回读和请求失败保留输入。
- 生产构建不注册夹具路由；正式操作按钮在审核 API 可用前保持禁用。
