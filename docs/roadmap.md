# 迭代路线与验收

初始迭代只承诺可安装、可运行、可继续开发的三端工程。实际验证结论见 [verification.md](verification.md)，以下已实现项不等于在每个真实平台都验收通过。

以下保留产品范围与实现状态；带任务 ID、主责、依赖及逐项验收的认领清单见 [四人模块计划](team-work-plan.md)。认领后用 GitHub Issue/PR 跟踪进度，合并实际功能时同步更新此处状态，不能把计划当成已完成。

## 初始化实现范围

- 单仓三端、各自前端 lockfile、Maven Wrapper、独立 MySQL Compose、环境加载和本地账号初始化。
- Campus Loop 管理布局、用户端发现首页/发布/详情、共用主题变量、控件实验室与覆盖说明。
- JWT 登录、数据库有效会话、退出撤销；用户发布和读取物品，管理员读取记录与统计。
- 无副作用的双方/三方有向循环匹配、流向与理由、虚构演示物品、边界测试。
- Flyway 迁移、虚构数据幂等插入、隔离后端测试、三种前端构建与 GitHub Actions。
- 开发协作、API、状态/事务、履历来源和迁移说明。

## B-01：独立需求清单与本人可提供物品关联

状态：已由 [PR #4](https://github.com/429res/campus-loop/pull/4) 合入 main，历史协作见 [Issue #3](https://github.com/429res/campus-loop/issues/3)。范围为本人需求 CRUD、分页、停用/逻辑删除、version 冲突、现有物品候选关联和 D-02 接入契约。需求允许没有物品，候选关联不占用、不改变所有权。需求迁移为 V4，保留 A-01 的 V3；A/D 候选基数复核仍待回复。分层验证见 [B-01 接入说明](b01-independent-demands.md)，本分支 D-02 用户端接入状态见 [需求清单说明](d02-demand-list-status.md)。

B-02 独立匹配进度见下一节；D-02 需求管理已合入，推荐理由页面在 PR #22 消费独立需求推荐。requiredTags/最低成色硬条件与正式交换仍未接入。

## B-02：独立需求与可解释匹配

状态：已通过 PR #11 合入 main；协作 [Issue #7](https://github.com/429res/campus-loop/issues/7)。新显式登录入口只使用独立 ACTIVE 需求及有效本人物品关联，返回需求命中、流向、分类/标签与规则版本；旧入口保持旧来源。分类仍是硬条件，requiredTags/最低成色未启用。本轮只读，无正式交换或占用。D-02 推荐理由接入见 [切片状态](d02-favorites-reasons-status.md)。

多需求确定性选择、D 样例和限额已同步。D-02 只消费服务端结果，不在前端重算规则；实际后端与隔离 MySQL 证据见 [B-02 说明](b02-independent-matching.md)。

## B-03 第一切片：领域入口与参与者读取

本功能分支完成严格命令、纯领域验证、B-02版本前置元数据和真实本人分页/参与者详情。A已确认没有A-03事务实现，唯一端口未注册，合法POST仍501；没有新占用表、迁移或第二套写入路径。用户已确认发起人不自动确认、DB UTC+24h、按发起人/键幂等、仅independent-v2及进行中需求冻结政策；正式创建、冻结落地和MySQL创建竞争验收须A-03接入后完成。确认/取消/交接/自动超时不在本切片，不标记整个B-03完成。范围、实际验证及给C/D样例见 [B-03说明](b03-exchange-domain.md)，协作 [Issue #25](https://github.com/429res/campus-loop/issues/25)。

## 下一迭代：闭合交换链路

| 负责人 | 工作 | 验收标准 |
| --- | --- | --- |
| A | UniApp继承工具链告警专项升级 | 跟随兼容的DCloud版本组合验证H5/微信，复核npm audit；外部部署前完成，不使用盲目force修复 |
| A | 注册/资料/密码变更、细粒度权限、账号审计 | 未登录401、越权403；密码变更撤销旧会话；数据库迁移兼容已有开发数据 |
| A + B | 独立“我有/我想要”清单 | 不依附物品也能保存需求；字段校验、owner权限、分页、停用；消费者同步契约 |
| B | 交换创建/确认/取消/超时 | 两个并发方案抢同件物品只有一个成功；无部分占用；重复请求幂等；到期释放仅本交换占用 |
| B | 交接及履历可信度 | 2/3参与者确认才完成；来源、证据、发生/记录时间可追踪；自述不能自动成为管理员核验 |
| C | 分类、审核、举报处理 | 查询、筛选、分页、审核冲突、理由必填；所有新增控件纳入共享材质和交互验收 |
| D | 收藏、需求清单、交换邀请/确认 | 无假成功，接口错误可恢复；H5与微信各自验证；真机图片上传/登录/交接路径可复现 |
| 全员 | 跨端集成和可访问性 | 全新clone复现；键盘焦点、移动长表单、空状态、快速弹层、深色与减少动态效果通过 |

### C-01 分批状态

- “账号查询与启停”管理端切片已接入 A-01 的真实分页查询、筛选、审计与版本化启停 API；前端实现和验证边界见 [c01-account-status.md](c01-account-status.md)。
- “物品审核”管理端只读切片和表单夹具已在功能分支实现，接入说明见 [c01-item-review.md](c01-item-review.md)。
- A-02 第四批提供真实物品审核、理由、审计与版本冲突，C审批接线及D提交/本人进度读取同PR交付。旧数据标记历史直发，新发布待审，B只消费AVAILABLE；见 [a02-item-review.md](a02-item-review.md)。C-01其他分类写接线、举报处理仍待完成。

### C-02 分批状态

- 本阶段新增控件已统一共享焦点、遮罩、禁用、动效与原生色值；管理端及用户端展示入口覆盖失败/加载/降级，H5 `LoopSheet` 补齐共享滚动锁、Tab圈定、Escape与焦点返回。协作范围见 [Issue #29](https://github.com/429res/campus-loop/issues/29)。
- 管理端/H5浏览器和三端构建已完成；微信产物检查通过，但开发者工具/真机以及D登录态下的D-03推荐预览仍待成员联调，因此不把后续新增业务控件或全部跨端平台验收视为永久完成。

### A-01 分批状态

- 第一批“本人资料与修改密码”已通过 PR #2 合入；用户端接入状态见 [D-01 切片记录](d01-profile-password-status.md)，字段、错误和会话规则见 [a01-profile-password.md](a01-profile-password.md)。
- 第二批“管理员查询与账号启停”已通过 PR #6 合入；成员 C 接入说明见 [a01-admin-user-status.md](a01-admin-user-status.md)。
- 第三批“注册准入和注册接口”已通过 PR #8 合入，采用默认关闭、仅显式开启的开发自助注册；字段、错误和成员D接入说明见 [a01-registration-admission.md](a01-registration-admission.md)。
- A-01 的更多个人资料、细粒度权限、完整账号审计及真实校园准入仍未完成；本切片完成不代表整个A-01完成。

## 后续业务

### A-02 第一批：本人物品管理后端

已通过PR #16合入的第一批实现按会话的本人分页/详情、完整编辑与 AVAILABLE → HIDDEN 下架、白名单/上传归属校验、版本冲突和占用/进行中交换保护；复用既有表结构，无新增迁移。接入与验证见 [a02-own-items.md](a02-own-items.md)，协作见 [Issue #15](https://github.com/429res/campus-loop/issues/15)。D 的表单和错误契约已同步，尚未完成 D 页面接入确认、H5/微信联调。重新上架、HIDDEN 编辑、审核、收藏、分类写入和正式交换不属于本批，整个 A-02 仍未完成。

### A-02 第二批：收藏持久化

已通过 PR #19 合入 main 的收藏后端实现本人收藏添加/取消/分页，V5唯一关系及非级联外键，复用公开物品可见性；不可见物品返回无内容占位，total包含所有本人关系。重复和并发操作、隔离MySQL约束验证及D接入说明见 [a02-favorites.md](a02-favorites.md)，协作见 [Issue #17](https://github.com/429res/campus-loop/issues/17)。本批未修改推荐算法或用户页面，D-02 收藏页面接入/消费确认及浏览器/微信联调仍未完成；已同步主线的物品管理PR #16；审核及正式交换仍未实现。

争议证据上传与管理员裁决、维修/流转事件、通知 outbox、统计口径、匹配候选索引/分页/缓存、风控及校园身份核验。外部 OSS 和微信登录由适配器接入，必须配置和验证后才开放入口。

## 当前明确未实现

公开注册或真实校园身份准入、超出 `displayName` 的个人资料编辑、交换交接前端联调、举报裁决、履历审核、外部通知、OSS、真实微信身份认证。相关页面只能显示“待开发”；后端预留操作返回明确501，不允许给出虚构业务成功。

C-01 分类维护已提供管理端真实读取、筛选/分页和开发环境表单夹具；A-02本分支已提供分类写接口、版本与引用保护，正式按钮等待C接线仍禁用，不能将 C-01 标记为完成。

### C-04 第一切片：举报受理与处理队列界面

管理端已提供生产待开发入口与仅开发环境注册的组件夹具，覆盖队列筛选/分页、目标失效、证据不可访问、受理/处理理由、防重复提交和 409 回读后保留理由。仓库当前无举报表、迁移或 API，正式入口不会用本地数组伪造成功；草案字段及 A/D 协作边界见 [C-04 举报队列说明](c04-report-queue.md)。A 的权限、持久化、受保护证据与审计，以及 D 的提交/本人结果展示均待实现，交换争议领域后果仍归 B；本切片不代表整个 C-04 完成。

## 四人每周协作节奏

每项功能先建立 Issue 和契约草案；A/B确认数据与事务，C/D确认消费字段和交互。每周一个可运行纵向切片通过 PR 合入 main。每位成员至少参与一次跨端联调、一次其他模块评审和一次干净clone验收；不使用人为目录权限割裂协作。

### A-02 第三批：管理员分类维护

本功能分支交付ADMIN分页/详情/新增/部分编辑、停用与启用、仅未引用分类删除，新增V6。物品两处分类与B-01需求分类（含墓碑）统一保护，新选择校验ACTIVE，历史名称可读；版本、唯一性和引用并发见 [接入与验证](a02-category-maintenance.md)。C/D/B已在 [Issue #20](https://github.com/429res/campus-loop/issues/20) 同步，消费确认待回复。本批只修复两端读取兼容与空分类草稿边界，C写表单接线及微信真机联调待后续；没有审核或交换写入、分类层级或匹配规则扩展。

### A-02 第四批：物品审核后端

本 PR 实现 ADMIN 状态筛选/详情/审核决定/审计分页、发布与编辑复审、V7 历史直发迁移、占用保护、版本冲突；C审批及D提交跳转、本人审核进度/原因读取同批兼容。双方候选读取沿用 AVAILABLE 过滤，不扩展算法。规则确认与协作在 [Issue #23](https://github.com/429res/campus-loop/issues/23)，实际验证与未联调项见 [交付说明](a02-item-review.md)。D本人完整编辑/下架表单、HIDDEN重新上架、正式交换状态机、微信工具/真机与成员环境联调仍待后续；不能把本批审核等同于整个A-02/C-01完成。

## A-03 当前切片

本分支通过B-03 PR #28的唯一端口实现正式创建、事务重校验、统一锁序、幂等、唯一占用和V8需求冻结/历史快照；服务集成与隔离MySQL验收见[A-03](a03-exchange-transaction.md)。B创建入口与本人读取已实际集成，确认/取消由下述B-03.2接入；交接仍待实现，自动扫描由A-04接通，D/C页面未接入本次写入口；不表示完整交换闭环完成。

## B-03 第二切片：参与者确认与取消

已集成A-03 PR #32，完成参与者独立确认、READY、取消审计、条件释放、允许动作以及共同内部到期入口，V9不改写旧数据。状态/权限/幂等与截止规则沿用已确认决定；见[B-03.2](b03-invitation-rules.md)及[Issue #30](https://github.com/429res/campus-loop/issues/30)。本片后端验证独立记录；D/C页面、交接/所有权转移、A-04定时扫描和恢复由独立切片补齐，不标记整个B-03或A-04完成。

## A-04 当前切片

基于B-03.2 PR #31共用生命周期事务，完成数据库UTC扫描、条件到期与释放、V10持久退避、独立JVM重启恢复验证。只新增扫描/运维边界，不复制取消/确认状态机；交接事实非空不得自动恢复物品。见[A-04](a04-exchange-expiry.md)，依赖PR按A-03→B-03.2→A-04审核合入，C/D页面和完整交接闭环仍未完成。

## B-04：交接与原子流转

本片实现独立交出/收到声明，全部参与者2N声明后原子转移所有权、关闭精确需求、追加参与者确认履历并释放自身占用；已开始交接可登记争议并停止推进。复用A-03/B-03/A-04共同服务，V11迁移，本片自动化及隔离MySQL验收通过，待PR审核。契约与实际证据见[B-04](b04-exchange-handoff.md)。D/C页面联调、争议裁决、履历修正和管理员核验未实现，不标记这些后续任务完成。

## B-05 第一切片：自述、证据与查询

自述维修/流转、合法作者范围、未知发生时间、追加修正链与分页/详情已接入，私有图片证据复用本地上传并按事件授权。V12不覆盖B-04真实交换事实；第一切片仅SELF_REPORTED写入口；参与者追加确认见下方第二切片，管理员核验及C/D页面联调未实现。本片自动化与隔离MySQL通过，待PR审核，见[B-05.1说明](b05-self-reported-history.md)。

## B-05 第二切片：参与者独立确认与来源链

已接入全体2/3人固定集合、不可变证据快照、独立确认/撤回及来源链读取；B-04事实直接复用，原自述不覆盖，修正不继承确认。自动化与隔离MySQL通过，待PR审核；管理员核验、C/D页面联调仍待后续。见[B-05.2](b05-participant-confirmation.md)。

## B-05 第三切片：管理员单事件核验

核验队列/详情、一次性版本决定、限定范围和证据快照的追加审计已实现，自动化/隔离MySQL通过，待PR审核。原自述和参与者确认保留，修正独立待核验；不变更owner/交换/占用，不裁决举报争议。前两片已合入，本片后端与C/D页面联调分开验收，见[B-05.3](b05-admin-verification.md)。
