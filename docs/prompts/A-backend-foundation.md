# A · 后端基础、认证与工程

本文件是待开发任务的提示词，不代表相关功能已完成。A 负责账号、基础 API、迁移和环境，并与 B 共用交换事务设计。依据：[团队规则](../../AGENTS.md)、[任务计划](../team-work-plan.md)。

每次只复制一个 `text` 块，在已拉取共同规则的仓库根目录开启任务。A-01 至 A-05 沿用任务计划编号；“第几批”只是拆分，不是新模块或完成状态。首个建议任务是 A-01 第一批；每批合入后再核对下一批依赖。

## A-01 第一批 · 本人资料与修改密码

```text
你作为 Campus Loop 成员 A，本次完成 A-01 第一批“本人资料与修改密码”。
读取根 AGENTS.md，按需阅读 docs/api-contract.md、docs/architecture.md 及现有用户、认证、会话源码。核对分支和 git status，保护已有改动，在独立 feature 分支工作。
先给简短方案，随后完成本切片实现和验证；沿用现有 Java/Spring Boot 分层、统一返回、JWT + 数据库会话、BCrypt。
范围：复用 GET /auth/me；允许本人修改 displayName 并从数据库回读；校验旧密码后修改密码，原子撤销该账号全部旧会话（含当前会话），要求重新登录。
身份从服务端会话确定，DTO 只接受允许修改的字段；不得通过客户端修改 id、username、role、status 或密码哈希。检查登录与改密的并发一致性，防止改密后仍保留通过旧密码取得的有效会话。
不在本批加入注册、校园认证、账号启停或额外个人信息采集。先同步新增写接口的字段与错误契约，保持现有登录/读取接口兼容。
验收：未登录拒绝；受保护字段不能篡改；资料重新查询仍存在；旧密码错误无副作用；至少两个原会话全部失效；旧密码不能登录、新密码可以；并发登录/改密符合相同规则。
运行相关自动化及隔离 MySQL 测试，不接成员日常库，不输出凭据或令牌。需要改表时新增 Flyway 迁移，不改写已共享迁移。
交付后端、契约、测试及给 D 的字段/错误/重新登录说明。更新本切片状态，不把整个 A-01 标记完成；提交功能分支和 PR，不自动合并，分别报告验证与未验证部分。
```

## A-01 第二批 · 管理员查询与账号启停

```text
你作为 Campus Loop 成员 A，本次完成 A-01 的管理员查询用户和账号启停接口。
先读根 AGENTS.md、任务计划中 A-01/C-01，以及相关认证和用户实现；核对已有改动，在独立 feature 分支工作，复用既有分页、鉴权和异常处理。
先核对 A-01 已合入的会话与改密行为，和 C 使用的查询/状态契约。实现管理员用户分页查询、筛选、启用/禁用及必要审计，不新增管理员赋权或硬删除用户能力。
服务端强制 ADMIN；禁用原子撤销用户会话并阻止继续登录，重新启用不能恢复旧令牌。禁用与登录使用一致并发策略。检查对物品可见性和匹配候选的现有规则，禁用用户不可参与推荐。
为当前管理员自我禁用、最后一个可用管理员等边界提出明确规则并写入契约；未定规则只阻塞相关写操作，继续查询和独立验证。不能通过禁用操作使系统失去管理入口。
验收：普通用户访问403；分页筛选正确；并发管理员操作不悄悄覆盖；禁用后旧会话和新登录均失效；重新启用需重新登录；审计不含口令/token，历史物品不被删除。
补自动化、隔离 MySQL 的会话/并发测试，给 C 提供请求响应和失败样例。只交付本批后端，不抢写管理页面；同步文档后提交 PR，不自动合并，说明实际验证结果。
```

## A-01 第三批 · 注册准入与注册接口

```text
你作为 Campus Loop 成员 A，本次推进 A-01 的注册准入和注册接口；从根 AGENTS.md 开始，仅展开相关任务、API、用户和认证代码，检查 git status 并使用独立 feature 分支。
先查 Issue/PR/契约是否已决定注册准入：开发自助注册、邀请码或校园身份核验。没有已确认策略时，给出简短取舍并请求这一个业务决策；继续完成字段、错误、测试方案等独立工作，不擅自开放公开注册，也不假称已核验校园身份。
按确认的策略实现最小注册：用户名唯一、输入校验、BCrypt 密码哈希、普通 USER 身份由服务端赋值；失败不能留下半成品账号。复用既有登录，不提供客户端 role/status 指定或管理员自助注册。
只采集已确认的必要字段。密码不回传、不记录；校园服务、短信或邮件若未授权配置，不接外部服务或伪造验证成功。
验收：正常注册可按现有方式登录；并发相同用户名仅一个成功，另一个返回契约错误；非法输入拒绝；客户端不能提权；关闭或不符合准入的注册被拒绝；失败不遗留部分数据。
同步 D 的表单字段、准入说明、错误和注册后行为；必要表结构用新增迁移。执行相关自动化与隔离 MySQL 唯一约束测试，提交后端、契约、测试和 PR，不自动合并；策略未定时明确剩余阻塞，不宣称注册已完成。
```

## A-02 第一批 · 本人物品编辑与下架

```text
你作为 Campus Loop 成员 A，本次完成 A-02 的本人物品列表、编辑与下架后端。
读取根 AGENTS.md，按需看物品、上传、匹配代码和 API/状态契约；检查已有改动，用独立 feature 分支，沿用现有服务、校验和返回。
先核对当前审核与交换状态是否已实现，明确哪些状态允许编辑/下架；不能让已占用、交接中的物品绕过交换状态机。默认不在本批引入审核状态或硬删除历史记录。
提供按会话限定的本人物品分页列表，不靠公开列表前端过滤代替权限。实现本人可编辑字段白名单、版本冲突检测、允许状态下的下架；owner 由会话确定。图片继续复用现有上传归属校验，不允许引用别人的上传或任意服务器路径。
验收：本人列表包含契约允许本人查看的状态且不泄露他人数据；修改后数据库回读一致；非本人拒绝；并发编辑出现明确冲突而非覆盖；不可操作状态拒绝；下架后不再作为推荐候选；既有发布/读取保持兼容。重新上架若不在已确认范围，明确留待后续。
同步 D 的表单和错误契约，补自动化及隔离 MySQL 状态/版本测试；仅新增必要迁移。完成本批后端、文档、测试并提交 PR，不自动合并，报告真实验证及剩余状态能力。
```

## A-02 第二批 · 收藏持久化

```text
你作为 Campus Loop 成员 A，本次完成 A-02 的收藏添加、取消及本人收藏列表。
先读取根 AGENTS.md、API 契约的收藏草案和相关用户/物品代码，核对分支与未提交改动，使用独立 feature 分支；不要把草案当作现有接口。
确定并更新 D-02 消费的字段、分页、物品不可见/下架时的呈现规则。实现基于会话的收藏关系、unique(user,item) 约束，以及重复添加/取消的幂等语义，不通过前端本地数组冒充持久化。
不要回传他人的私有收藏或绕过物品可见性检查；列表规则和计数口径写入契约。表结构采用新增 Flyway 迁移。
验收：添加后刷新与重新登录仍可读取；重复或并发添加只有一条关系；重复取消结果稳定；用户只能访问自己的收藏；无效/不可见目标按契约处理；分页空结果正确。
运行相关自动化与隔离 MySQL 约束/并发测试，给 D 提供接入说明。提交代码、契约、测试和本批 PR，不自动合并，不扩展到推荐算法或用户页面。
```

## A-02 第三批 · 分类维护 API

```text
你作为 Campus Loop 成员 A，本次完成 A-02 的管理员分类维护 API，供 C-01 使用。
先读根 AGENTS.md 和相关分类、物品、需求代码与契约，核对已有工作并使用独立 feature 分支。复用 ADMIN 校验、分页、参数校验和错误处理。
先同步分类新增/编辑/停用或删除的明确语义。当前已存在物品或需求引用时，不能直接删除造成悬空引用；分类唯一性、排序、停用后发布与需求选择规则按确认契约实现，不额外引入无限层级分类。
考虑 B-01 新需求表是否已合入，统一维护引用约束；新旧客户端分类读取保持可预期。新增必要迁移，不改写已有版本。
验收：ADMIN 可维护、普通用户403；非法和重复输入处理明确；引用中分类受保护；并发修改/删除不破坏引用；停用分类不再被新业务选用，历史物品仍能解释分类。
补相关自动化与隔离 MySQL 约束测试；给 C/D/B 提供字段与影响说明，受影响前端读取需要兼容时同步处理。提交本批 PR，不自动合并，说明实际测试范围。
```

## A-02 第四批 · 物品审核后端与状态迁移

```text
你作为 Campus Loop 成员 A，本次完成 A-02 的物品审核后端，和 C-01、D-01、B 的候选读取协作。
按根 AGENTS.md 检查工作区，仅阅读相关物品/匹配/审核/迁移代码及契约，在独立 feature 分支工作。先核对审核发布规则是否已由团队确认。
当前初始工程直接发布 AVAILABLE；要引入 PENDING_REVIEW，必须先明确新发布、编辑后复审、驳回再提交及旧 AVAILABLE 数据如何处理，并同步前端状态和匹配过滤。关键规则未知时先提出具体决策，继续独立的查询、DTO和测试工作，不静默重解释旧数据。
实现 ADMIN 审核查询/决定、理由、审计和版本冲突；普通用户不能自行设置审核结果。审核不会越过物品占用或交换状态，保留原始记录。
验收：待审/驳回物品不进入可交换推荐；普通用户无法审批；两个管理员并发决定不会覆盖；状态变化按契约可追溯；旧演示和已发布数据经迁移后行为清楚；前端能正确处理新状态。
新增迁移与相关隔离 MySQL 测试；给 C/D/B 提供变更样例，兼容改动在同一可审核发布范围完成。提交本批代码、文档、验证和 PR，不自动合并；未联调的端单独标注。
```

## A-03 · 交换创建的事务与占用基础

```text
你作为 Campus Loop 成员 A，本次完成 A-03，与 B-03 共用交换创建服务，不另建一套交换状态机。
读取根 AGENTS.md、架构的事务设计、API 契约、交换预留迁移和实际物品/匹配代码，核对 B-03 的接口/服务边界与分支改动；使用独立 feature 分支。
先确定 A 负责的事务服务与 B 的创建入口如何集成；契约未定只澄清关键接口边界，继续可独立完成的约束与测试。推荐方案是输入线索，不能视为物品仍可交换的证明。
在同一事务内按统一物品顺序锁定并重校验用户、物品、有效需求、环长2/3及流向；创建交换/参与者、唯一占用并更新物品状态。复用领域规则，creator 从会话确定且必须是有效参与者。
实现 (creator,idempotencyKey) 的幂等约束；同键不同请求内容应明确冲突，不能返回不相关交换。任一校验或唯一约束失败整体回滚，转换为契约中的冲突错误。
验收：同件物品两个并发方案仅一个成功；无部分占用/参与者；同请求重试得到同一交换；同键不同请求拒绝；过期推荐不可直接创建；越权和伪造参与者拒绝；推荐 GET 仍无写入。
必须执行隔离 MySQL 并发/回滚测试，不能用 H2 代替行锁证据。必要结构仅新增迁移，与 B 共用锁序/状态规则，交付服务接口和失败样例；提交 PR，不自动合并，明确 B 集成是否实际完成。
```

## A-04 · 超时、竞态与重启恢复

```text
你作为 Campus Loop 成员 A，本次完成 A-04 的交换超时处理和恢复，依赖已实现的 A-03/B-03。
按根 AGENTS.md 核对实际分支、状态机、创建/确认/取消事务及时间字段，只展开相关代码，在独立 feature 分支工作。没有可用交换服务时先补清依赖契约和测试设计，不写独立的重复状态机或假任务成功。
实现基于数据库 UTC 时间的到期扫描、可重试批次、条件状态更新和释放；取消、超时、确认、交接共用锁顺序与合法转换，只释放属于本交换的占用。
明确哪些状态可超时。已经交接的物品不能因普通超时重新上架；实例重启后继续处理已到期记录，不依赖单机内存定时器保存唯一任务状态。
验收：重复任务幂等；取消/确认与超时并发结果合法；不释放他人或新交换占用；无部分释放；重启后恢复；数据库时间而非前端倒计时决定结果；异常可重试且不泄露凭据。
用隔离 MySQL 执行竞态及恢复检查，给 B/C/D 同步截止时间和结果语义。更新运维/契约说明、提交功能分支 PR，不自动合并，分别报告单元、数据库和运行恢复的实际证据。
```

## A-05 第一批 · UniApp 工具链兼容升级

```text
你作为 Campus Loop 成员 A，本次只处理 A-05 的 UniApp 工具链依赖告警与兼容升级，D 协助平台验证。
先读根 AGENTS.md、README、docs/verification.md 中告警基线及实际 package.json/lockfile，核对工作区，在独立 feature 分支工作；先运行当前审计确认现状，不照抄旧告警数字。
根据当前 DCloud 官方兼容资料制定最小升级组合，保留各端独立构建器，不执行盲目的 audit fix --force，不在本 PR 改业务。说明必须变动的直接和传递依赖；风险修复不可兼容时列出准确剩余项和下一步。
验收：npm ci 可重现；H5 与 mp-weixin 构建通过；用户端登录/发布/上传及公共控件无回归；D 在可用微信工具/真机中验证，无法验证则明确标注，不能用 H5 代替。管理端仅在实际受影响时补相应检查。
更新锁文件、版本/运行文档和最新审计结果，记录兼容性依据与已执行验证；通过 PR 交付，不自动合并或外部部署，不把未修复告警宣称已解决。
```

## A-05 第二批 · 备份、恢复与部署配置

```text
你作为 Campus Loop 成员 A，本次完成 A-05 的备份恢复演练和部署配置说明。
按根 AGENTS.md 检查分支与已有脚本，只读相关 Compose、环境加载、Flyway、上传配置和文档，使用独立 feature 分支，沿用跨平台运行方式，不更换整个工程环境。
在独立临时库和独立上传目录中设计并实际演练备份/恢复；同时覆盖数据库与上传引用，不把导出 SQL 成功当成可恢复。不得覆盖成员日常库，不将备份、凭据或上传提交到 Git。
核对开发与部署环境的 API 地址、上传路径、JWT/数据库密钥加载、跨域和反向代理说明。部署目标未提供时先完成可验证的配置模板与本地演练，不自行购买服务、发布站点或修改外部权限。
验收：向新隔离库恢复后，虚构物品、需求和可用上传引用一致；迁移状态正确；应用能读取恢复的数据；重复演练步骤明确；Windows/macOS 命令差异准确，未实测平台说明。
交付可重复步骤、必要脚本、清理边界和实际恢复证据，文档只用虚构数据/公开配置。提交功能分支 PR，不自动合并或执行未经授权的部署。
```

## A-05 第三批 · 登录/注册/上传限流

```text
你作为 Campus Loop 成员 A，本次推进 A-05 的认证与上传限流，范围限定为已实际存在的高成本或身份接口。
从根 AGENTS.md 和相关接口/异常/部署代码开始，核对分支与已有实现，使用独立 feature 分支。先明确部署是单实例还是多实例，以及代理来源可信边界；未知项先记录并给出需要的最小决策。
按已确认范围实现可配置限流、明确错误和恢复时间语义，合理处理匿名请求、登录用户和代理地址；不泄露账号是否存在，不永久锁死用户。若需要新中间件，作为明确的架构依赖变更说明理由和本地服务方案，不悄悄依赖共享 Redis。
验收：正常请求不受误伤；超限按契约拒绝；窗口结束后恢复；并发请求计数有效；配置能被实际启动命令读取；伪造转发头不能任意绕过；测试使用可控时钟或隔离环境。
给 C/D 同步错误与恢复行为，运行受影响测试并按真实部署范围验证，提交代码、配置、文档和 PR，不自动合并；单实例验证不能宣称覆盖多实例一致性。
```

## A-05 可选批次 · OSS 或通知适配（一次选一种）

```text
你作为 Campus Loop 成员 A，本次只推进 A-05 中团队已明确选定的一种外部适配：OSS 上传或通知 outbox。
先读根 AGENTS.md 和相关存储/交换事务代码，核对分支及已有改动，使用独立 feature 分支；核实任务或 Issue 是否已明确选择、服务商与授权范围。未确定时先给出两种能力的最小接口边界供决策，继续独立测试设计，不同时实施两种服务，不对外发送测试通知。
选择 OSS 时：沿用上传校验、归属与存储接口；密钥只由后端读取；未启用时本地文件上传仍可运行。验证切换、失败恢复和可见 URL，不假定填入凭据即成功。
选择通知时：领域事务只写 outbox，提交后发送；定义幂等键、重试和重复投递处理。先使用本地可观察适配器验证，真实收件人/渠道发送须有对应明确授权。
仅实施已选分支；新增依赖/配置说明理由与加载方式，不将密钥写入文档。外部条件缺失时可以交付本地适配和测试，但必须标注真实服务尚未验证。
提交本批代码、契约、配置说明及相关验证的 PR，不自动合并、部署或更改账号权限。
```

## C-04 协作 · 通用举报与处理审计后端

```text
你作为 Campus Loop 成员 A，协作 C-04/D-04 完成通用举报的后端接口；C 负责管理页面，D 负责用户提交，B 负责交换争议的领域后果。
从根 AGENTS.md、C-04 任务、相关 API/模型及现有权限/上传代码开始，核对工作区和已存在实现，使用独立 feature 分支。先同步本次举报目标类型、本人查询、管理员队列/决定、状态/版本、证据可见范围与错误契约。
实现提交、本人读取、ADMIN 受理/处理及追加审计，复用现有鉴权、校验、分页和上传归属；理由和目标有效性由服务端校验，客户端不能指定举报人或处理身份。处理重复请求和并发决定，敏感证据不能进入公开列表。
普通举报决定不会自动修改交换所有权、释放占用或完成裁决。需要交换争议后果时调用 B 已确定的同一领域服务；B 未实现时明确该动作尚不可用，不能借举报接口绕过交换状态机。
新增必要迁移并协调序号；验收覆盖真实提交后本人和管理端各自回读、非法目标/他人证据/越权拒绝、重复提交、两个管理员冲突、失败回滚和审计保留。使用隔离 MySQL 与虚构数据验证，不接日常库、不输出凭据或真实举报材料。
交付给 C/D 的字段和失败样例，记录与 B 争议服务的边界、已接通与未接通能力。提交后端、契约、测试及本协作切片的 PR，不自动合并，不把整个 C-04 标记完成。
```
