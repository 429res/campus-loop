# D-03 从推荐发起交换、我的交换、邀请确认与取消

更新日期：2026-09-08。本切片已核对 main、开放 PR、服务端控制器及测试；B-03/A-03 尚未实现，因此当前只交付真实推荐的发起前预览和明确待开发入口，不接入 501，不产生交换、占用或幂等键。

## 已交付

- 推荐卡片提供“发起前预览”，抽屉直接消费 B-02 `independent-v2` 的参与者、提供者→接收者物品流向、`demandId`、分类和理由；预览、关闭与刷新均为只读。
- “我的交换”页面及个人页入口已接线。未登录时进入统一登录恢复路径；已登录时明确说明不存在可调用的本人交换接口，不展示本地伪记录。
- 正式发起、邀请确认和取消均保持禁用。页面不会调用现有 501 预留路径，不生成或持久化猜测的幂等键，也不根据推荐自行推导交换状态。
- 抽屉复用 `LoopSheet` 的可中断动效、Escape、焦点圈定与返回；手机布局、深浅色、减少动态及滤镜降级继承公共 token 和材质。

## 给 B/A 的唯一依赖反馈

请 B-03 与 A-03 在同一可调用契约中确认并实现以下内容后再开放按钮：

1. `POST /api/exchanges` 的最终请求 DTO，以及推荐标识、规则版本、物品顺序和 `Idempotency-Key` 位于 header 还是 body；同一逻辑提交的恢复查询方式必须明确，不能只依赖创建响应。
2. `GET /api/exchanges` 本人分页、`GET /api/exchanges/{id}` 参与者详情，以及服务端返回的 `status/version/allowedActions`、参与者确认状态、物品流向和 UTC 时间字段。
3. confirm/cancel 的最终 DTO、重复操作响应、409 分类（推荐失效、占用、版本、已推进）与 403 非参与者语义；结果不确定时应按哪个稳定标识回读。
4. 创建事务锁序、唯一占用、幂等唯一约束与取消仅释放本交换占用，由 A/B 隔离并发测试证明。D 不复制这些规则到前端。

当前 `docs/api-contract.md` 第“后续接口设计”仍是草案，`SystemController` 对 exchange 写路径统一返回 501，且没有列表/详情 GET。验收中的双方/三方推进到 READY、重复创建/确认、非参与者拒绝、占用冲突、取消回读因此未运行，不能宣称完成。

## 验证记录

- 构建：在 `wx-project-self` 运行 `npm run build:h5` 与 `npm run build:mp-weixin`，均通过；微信生成物接线检查 6 项通过。
- 自动化：用户端 37 项通过，其中新增 2 项确保推荐预览和“我的交换”入口不会请求 exchange 接口或伪造记录；`node scripts/run.mjs test` 的匹配 42 项与 H2 API 67 项通过，现有测试继续确认预留写入口返回 501。
- H5 浏览器：实际打开 720×900 的“我的交换”页，检查未登录恢复路径、浅/深主题切换、稳定内容表面和无页面级横向溢出。由于没有 B-03 列表/详情，也未在浏览器输入本机凭据，真实推荐抽屉、登录后依赖说明及交换动作未运行。
- 真实服务/数据库：没有交换业务 API 或 A/B 并发测试可运行；未执行 MySQL 交换验收，也没有数据库写入。收藏/推荐既有测试不能替代 D-03 验收。
- 微信：编译和生成物接线通过；微信开发者工具与真机未运行。
