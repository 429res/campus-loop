# 验收记录

## 2026-09-09：C-04/D-04 通用举报后端切片

- 新增 V15 通用举报、私有证据关联和追加审计；真实 HTTP/服务测试使用虚构账号、ITEM、图片及 DISPUTED 交换夹具覆盖提交、本人/ADMIN 回读、受理、决定、审计和证据内容授权。H2 目标套件执行 4 项通过，1 项 MySQL 专用行锁用例明确跳过。
- `node scripts/mysql-test.mjs --reports` 启动随机凭据的一次性 MySQL 8.4.11，从空库应用 V1–V15；ReportIntegrationTest 5/5 通过。真实两连接验证同一提交幂等串行、两个管理员只能一个决定；约束故障注入证明状态更新与审计一起回滚。容器在测试后自动停止删除，未连接成员日常库且未输出凭据或 token。
- 权限/隐私覆盖未登录、非 ADMIN、他人本人详情/证据、非法或失效目标、受保护字段、他人/重复证据；列表无全文理由和证据，详情不回传 upload UUID、路径、摘要、处理人 ID、幂等键、口令或 token。文件缺失/变化显式 MISSING/404。
- 普通 UPHELD/DISMISSED 只更新举报表，测试逐行确认目标 item、DISPUTED exchange 与 item hold 未改变。EXCHANGE 举报目标拒绝，B 的管理员争议裁决后果服务尚未实现或接通；C 管理页面及 D 提交/本人结果页面没有在本后端 PR 中改动，未执行浏览器、H5、微信开发者工具或真机联调，不能据此标记整个 C-04/D-04 完成。
- `node scripts/run.mjs test` 最终通过：用户端 53 项、后端公共规则 75 项、API/集成 155 项（23 项真实 MySQL 专用用例在 H2 运行中明确跳过），无失败；V15 在各隔离 H2 测试库与既有模块共同迁移。第一次沙箱内运行因 Windows 禁止 Node 测试子进程而统一报 `spawn EPERM`，获准在正常进程环境重跑后通过，该次不是代码断言失败。`node scripts/repository-check.mjs` 对暂存后的 402 个交付文件通过，无环境文件、依赖目录、构建产物或上传进入 Git。

## 2026-09-08：D-04 微信平台准备度与环境缺口

- 合并主线 `eac04e2` 后在 macOS 独立工作树复验：全新 `npm ci --offline=false`、用户端 53 项测试、H5/微信构建、微信按钮产物 6 项及平台准备度检查全部通过。页面清单补入主线履历和举报/争议入口，共 13 页；保留主线功能记录及本分支平台边界。以下 Windows 记录属于原分支验证，本次未运行浏览器、微信工具、真机或真实服务联调。
- Windows 工作区使用 Node 24.20.0、npm 12.0.2、UniApp 3.0.0-5020420260813003 和 Vite 5.2.8。本次重新执行用户端自动化49项、H5生产构建、微信生产构建、微信按钮产物检查6项和新增平台准备度检查，均通过；平台检查只输出AppID/API是否配置及地址类型，不输出配置值。
- 微信产物确认11个页面、原生TabBar、图片选择/上传/取消、picker、modal、导航主题、实色材质与减少动态效果接线，且控件实验室未混入H5 DOM调用。构建仍保持`urlCheck: true`；未配置真实AppID时产物为`touristappid`，小程序API未配置时为回环默认值。
- 本机未发现可调用的微信开发者工具、成员权限、真机或ADB，也没有可达隔离服务；Docker daemon不可用。因此本次没有运行开发者工具、真机、真实API/数据库或H5浏览器，不能验证授权拒绝/取消、弱网、前后台切换、导航返回、键盘焦点、系统深浅色及关键写入回读，也不把构建结果作为这些项目通过。
- 本次没有证实需要修改的微信运行时缺陷，未改业务、认证、工具链或锁文件。完整设备/功能矩阵、未实现能力及复现真实验收所需的最小条件见 [D-04 微信平台记录](d04-wechat-platform-verification.md)。

## 2026-09-08：D-04 履历、自述与用户争议入口

- `wx-project-self` 执行 `corepack npm test`：53 项通过；新增静态回归覆盖真实履历/私有证据/参与者确认路径、争议只服从 `allowedActions`、结果不确定恢复，以及未实现举报不发送请求。
- `wx-project-self` 执行 `corepack npm run build:h5` 与 `corepack npm run build:mp-weixin`：均通过。微信开发者工具和真机未运行，不能以构建替代。
- 根目录执行 `node scripts/run.mjs test`：用户端 53 项通过；后端模块测试 72 项通过，API 测试 148 项通过、22 项 MySQL 专用用例跳过。`node scripts/repository-check.mjs` 通过。
- 尝试启动项目隔离 MySQL，但本机 Docker Desktop daemon 未运行；默认后端随后因 MySQL 连接被拒绝而停止。因此本轮未进行真实 API/数据库写入、双方/三方页面确认、越权、刷新回读或争议数据库验证，不复用 B-05/B-04 既有记录冒充本轮证据。
- H5 开发服务已成功编译并监听本机地址，但本轮浏览器控制被环境审批服务阻止，未完成人工手机/平板、浅深色、键盘焦点、快速弹层、减少动态及滤镜降级检查。通用举报与管理员争议裁决 API 不存在，保持待开发。

## 2026-09-08：C-04 业务统计口径与管理看板切片

- 生产概览继续读取真实 `GET /api/admin/stats`，另以 `GET /api/admin/items?status=PENDING_REVIEW&page=1&size=1` 的服务端 `total` 展示待审量；没有从当前页推全量。统计、待审量、最近物品独立失败，真实0、推荐超限 null 和请求失败分别显示。
- A/B 源码核对确认：users 是全部账号、items 是全状态物品、availableItems 仅是当前 AVAILABLE、recommendations 是 legacy-v1 即时候选环而非交换成果；当前没有 ADMIN 交换统计、日期筛选、强一致 `asOf` 或趋势接口。契约已记录去重单位、范围、UTC 后续窗口建议及缺失完成事件边界。
- 管理端 Node 单元测试 17/17 通过；Vite 8.2.2 生产构建通过，仅有既存大 chunk 提示；根 `repository-check` 通过。统计相关 `MatchingLimitsIntegrationTest` 在隔离 H2 / 14 个 Flyway 迁移上 3/3 通过，验证基础计数、真实0和两类规模超限。根 `node scripts/run.mjs test` 先完成用户端49/49，随后因当前 Windows Maven Wrapper 的 `$HOME/.m2` 路径解析失败中止；改用同一 Maven 3.9.16 并显式指向本机依赖缓存后完成上述目标测试。
- 内置浏览器实际检查开发路由 `/fixtures/stats`：真实0显示0；推荐超限显示“—”及原因；待审量单路失败和全部失败均显示“读取失败”而非0。统计说明可用键盘 Enter 展开，浅/深色均检查；1280×720、820×1180、390×844 下 `document/body.scrollWidth` 未超过可视内容宽度，卡片分别为5列、3列、1列。减少动态效果开启后卡片/说明计算过渡均为0s，并已恢复原偏好与原深色主题；控制台无 error/warning。
- 浏览器场景使用固定虚构数据，仅证明组件状态与响应式，不是 API/数据库证据。本切片未改后端或数据库，未运行隔离 MySQL；上述 H2 集成测试是当前统计 API 的实际回归证据，新增完成交换/状态分布仍待 A/B 接口与数据库验证。

## 2026-09-08：A-05 UniApp 工具链兼容补丁

- 冲突修复复验：合入主线 `777d422`，完整保留 C-04 两组验收记录；在独立工作树使用 Node 24.20.0 / npm 12.0.2 全新执行 `npm ci --offline=false`、用户端测试 49 项、H5 与 mp-weixin 构建、微信产物接线检查 6 项及仓库交付检查，全部通过。依赖与锁文件保持 A-05 补丁原样；业务源码与上述主线一致。本次未运行浏览器、微信开发者工具或真机。
- 在 Node 24.20.0、npm 12.0.2 下显式联网执行 `npm audit`，升级前为 65 项（14 high、17 moderate、34 low、0 critical）；本机 npm 的离线缓存曾返回 0，未采用该错误结果。
- 保持 DCloud 5.24 正式版 `3.0.0-5020420260813003` 与其严格 peer `vite@5.2.8`，未切换 5.25 Alpha、Vite 8 或执行 `audit fix --force`。Rollup 更新至同一主版本内的 4.63.1，并对 Intlify 9、Express 4、PostCSS 8、qs 6、ws 8 使用已验证的补丁版本；移除源码和脚本均未使用的 `@dcloudio/uni-automator`。npm 12 安装脚本策略仅批准锁定的 `esbuild@0.20.2`，明确拒绝非必要的 core-js 提示脚本。
- 更新后联网审计为 39 项（4 high、10 moderate、25 low、0 critical），不是“全部解决”。剩余高风险由 DCloud 正式版固定的 Vite/esbuild/Babel/adm-zip 链及 Jimp/jpeg-js 链传播；其修复需要越过当前 DCloud peer 或 0.x 兼容边界，留待上游正式版组合。
- `npm ci` 可从锁文件重装 602 个依赖；用户端 Node 自动化 49 项、H5 生产构建、mp-weixin 生产构建及微信产物连线 6 项通过。首次沙箱内运行因 Windows 子进程权限 `EPERM` 失败，获准在沙箱外重跑后通过，该失败不是代码或依赖错误。
- 本切片未改业务、管理端或后端，故未重复管理端构建。未启动真实后端或浏览器，登录/发布/上传由既有 49 项会话与请求竞态自动化覆盖，公共按钮同时覆盖源级行为和微信构建产物；未执行微信开发者工具/真机验证，须由 D 按 [A-05 验证说明](a05-uniapp-toolchain.md) 完成，H5 结果不替代该结论。

## 2026-09-08：C-04 交换争议管理端追溯切片

- 页面与开发夹具覆盖双方/三方持久流向、交接声明、长争议理由、事件、加载/错误/空态和 DISPUTED 当前影响；正式入口不调用参与者私有 API，裁决按钮保持待开发。
- 管理端 Node 测试 13/13、Vite 生产构建、根 `node scripts/repository-check.mjs` 与差异空白检查通过；构建仅有既存的大 chunk 提示。本分支没有后端改动，未重跑后端或 MySQL。
- 内置浏览器实际检查开发夹具：三方长理由纯文本、参与者/流向/事件、错误→恢复、加载/空态入口、详情快速开关与 Escape。1280×720、820×1180、390×844 下 document scrollWidth 等于 clientWidth；浅色/深色、减少动态效果（导航 transitionDuration 0s）通过，浏览器 console 无 warning/error。关闭后无可见 overlay；临时视口和偏好已恢复。
- B-04 交付文档记录的团队后端证据为隔离 MySQL 199 项通过，包含参与者争议登记与最后交接互斥、保留 owner/hold/demand 及事件原子性；这是既有 B-04 证据，不是本分支重新运行，也不证明尚未实现的管理员裁决。真实 C/D 管理裁决联调、两个管理员并发和裁决失败回滚均未执行。

## 2026-09-08：C-04 举报受理与处理队列管理端切片

- 管理端 `/reports` 明确显示举报后端待开发，不请求不存在的接口；开发路由 `/fixtures/reports` 提供匿名化固定夹具，覆盖筛选/分页、目标失效、证据不可访问、受理/处理请求体、重复提交锁和 409 显式回读保留理由。
- 夹具捕获请求后不更新本地数组，不把界面状态当数据库成功。现有公开 `/uploads/**` 未用于举报证据，页面不渲染服务端路径或任意外链。
- 管理端 Node 单元测试 14/14 通过，`vite build` 通过；根 `node scripts/repository-check.mjs` 通过。系统全局 `npm` 启动器缺失，因此测试/构建使用工作区随附 Node 24.20.0 直接运行相同 Node test 与 Vite 入口；构建仅有既存的大 chunk 提示。
- 内置浏览器实际检查开发夹具：空理由错误、成功请求体捕获且队列不变、409 后禁用提交并显式回读 v2→v3、理由保留、目标失效、证据不可访问、详情/受理/处理弹层及 Escape。1280×720、820×1180、390×844 下 document/body scrollWidth 均等于 clientWidth；浅色/深色切换正常，减少动态效果时导航与抽屉 transitionDuration 均为 0s，关闭后 overlay 均为 display:none。浏览器偏好与临时视口已恢复。
- 仓库无举报 API/迁移/数据库表，因此未执行真实提交、权限、并发、审计或数据库验证；D 本人入口仍待实现，交换争议后果不在本切片。

## 2026-09-08：A-02 收藏持久化后端

- `node scripts/run.mjs test`：UniApp Node 26项、后端97项（42匹配 + 55 H2 API集成）全部通过，0失败/错误/跳过。
- `node scripts/mysql-test.mjs`：全新临时 MySQL 8.4.11、V1–V5迁移成功，同一97项后端测试全部通过，55项API集成实际连接MySQL；脚本使用随机本地凭据且不输出，容器自动停止移除。
- 新增10组收藏集成用例覆盖刷新/重新登录、重复/8路并发添加唯一且保留时间、重复/并发取消、会话隔离、管理员限制、全状态可见性、不可见占位/计数/分页、非法输入/过期会话、等锁后状态复核、数据库唯一约束/非级联外键。
- 仓库交付、差异空白及文档链接/JSON示例检查通过。未修改推荐算法和用户页面，未做本批浏览器/微信工具/真机联调，未连接日常库；同步PR #16后补充MockMvc及数据库的真实下架接口衔接验证，其他状态边界采用隔离夹具，D-02消费确认与用户界面仍待完成。详见 [收藏接入说明](a02-favorites.md)。

## 2026-09-08：A-02 本人物品管理后端（已同步 B-02 主线）

- `node scripts/run.mjs test`：UniApp Node 测试26项、后端87项（42匹配 + 45 H2 API集成）全部通过。
- `node scripts/mysql-test.mjs`：临时 MySQL 8.4.11 容器、V1–V4迁移成功，同一87项后端测试全部通过；45项API测试实际连接MySQL。脚本随机生成本地口令且不输出，完成后自动停止/移除容器。
- 新增8组物品用例覆盖会话限定分页/六状态/越权、字段与图片归属、数据库回读、编辑/下架版本竞态、等待锁后的状态/归属/占用/交接引用复核、下架退出推荐、需求关联及履历保留。交换记录由隔离测试夹具建立，正式交换 API 仍未实现。
- 仓库交付检查、差异空白检查与本批文档链接核对通过。没有本批页面改动或新增迁移；未运行浏览器、微信模拟器/真机及日常服务库联调。D 表单/错误契约已同步，消费确认及页面接入待后续。详见 [A-02记录](a02-own-items.md)。

## 初始化及此前记录

验收日期：2026-09-07。以下按层记录，不能把构建成功替代浏览器或微信真机验证。

| 层次 | 当前证据 |
| --- | --- |
| 工具版本 | macOS arm64，Node24.20.0、npm12.0.2、Temurin17.0.20.1、Maven Wrapper3.9.16实际运行 |
| 管理端 | npm ci与Vite8.2.2生产构建通过；控件页浏览器交互与响应式已执行 |
| UniApp | npm ci、H5及mp-weixin构建通过；H5实际登录、上传、发布、详情刷新与退出验证通过 |
| 后端默认测试 | 8项纯匹配测试 + 6项Spring/Flyway/Mapper集成测试通过，H2完全隔离 |
| MySQL测试 | scripts/mysql-test.mjs随机口令临时MySQL8.4.11容器，14项测试通过，运行后停止测试容器 |
| 开发数据库 | 独立campus_loop_dev/3308初始化成功；Flyway2个版本迁移；示例发布、读取、管理员查看与退出撤销14项API检查通过 |
| 管理端浏览器 | 桌面及375/390/768像素无横向溢出；浅/深色、输入错误恢复、日期、选项、分页、通知、弹窗/抽屉快速反向检查 |
| 干净clone | 从提交内容clone到全新目录：npm ci、三种前端构建、后端14项测试、另一独立MySQL初始化与14项API链路检查通过；生成manifest无需预先存在 |
| GitHub | 目标私有仓库429res/campus-loop，main；当前CI结果见[仓库Actions](https://github.com/429res/campus-loop/actions/workflows/ci.yml)（包括Linux和Windows构建），组织Free不支持私有分支强制保护 |
| Windows | 提供mvnw.cmd、跨平台脚本与Windows CI完整构建；本机未运行Windows桌面或Docker Desktop，云端结果以Actions为准 |
| 微信 | 小程序编译通过；未使用真实AppID，未执行开发者工具模拟器或真机联调 |

## 实际修复

浏览器检查发现Element Plus多个弹层快速切换可能残留body滚动锁，已改为公共引用计数锁，验证关闭后html/body无残留锁与可见遮罩。UniApp H5原生button的键盘语义不足，使用共享可访问按钮组件适配；微信保留原生实现。

测试隔离防止环境变量覆盖：即使外部SPRING_DATASOURCE_URL、SPRING_FLYWAY_URL存在，测试也强制使用已校验的H2或明确的本机campus_loop_*test连接。Windows脚本保留原生参数边界，避免带空格路径被shell拆开。

CI 的 `setup-java@v4` 不接受四段 Java 版本。已按 [Temurin 官方发布元数据](https://github.com/adoptium/temurin17-binaries/releases/download/jdk-17.0.20.1%2B1/OpenJDK17U-jdk_x64_linux_hotspot_17.0.20.1_1.tar.gz.json)使用精确 SemVer `17.0.20+101`（对应实际 `17.0.20.1+1`），Linux/Windows 均额外检查运行时版本和 Eclipse Adoptium 厂商；JDK 基线没有降级。修复后云端执行结果以 Actions 为准。

## 边界与已知提醒

- 匹配上限200件AVAILABLE候选、1000条推荐，任一超限返回422；正式规模化需候选分区。
- Flyway10.20.1对MySQL8.4输出“更新支持版本”的提醒；本次独立MySQL8.4.11迁移与集成测试实际通过。依赖升级需单独兼容性PR。
- 管理端完整引入Element Plus的生产包会有chunk体积提醒；构建成功。后续可按需拆包，避免初始化时盲目升级。
- API与浏览器示范有意保留两个虚构发布记录及测试图片（主开发库#2007、#2008），用于刷新与后台查看。它只存在本机开发库/上传目录，不进入Git；每次再次运行会再新增一条，不是无写入健康检查。
- 当前Exchange和履历为数据/接口设计与明确待开发入口，没有完整交易执行能力。

## 跨端浏览器和数据库证据

H5用本地生成的学生账号登录，原生选择分类/成色/需求、上传PNG、发布#2008；详情硬刷新仍显示同一标题和已加载图片。管理端本地管理员登录后搜索“浏览器验收 · 校园循环”，详情确认同一id、标题、标签和需求。退出后刷新保持未登录。未输出任何账号口令或token。

主开发库只读核对：5名用户（3名虚构演示者+2个本地账号）、8件物品、2个成功迁移；cl_item_hold=0、cl_exchange=0，推荐读取没有建立交换或占用。

H5六路由在375px与768px均无横向溢出，详情另行同尺寸验证；模态连续开关5次、抽屉6次，无阻挡遮罩，键盘焦点可返回。两个前端的应用内减少动态效果开关实测过渡0s；OS媒体查询使用共同降级规则，但未修改本机系统偏好，不宣称已切换原生OS设置。

## 依赖审计边界

2026-09-07 npm audit发现继承的UniApp工具链65项依赖告警（14高、17中、34低，无critical；包含传递/元依赖重复传播，非65个独立漏洞）。保持Vite5.2.8和匹配的DCloud锁定版本以保证本次H5/微信构建兼容；专项升级在路线图中列为外部部署前要求。开发服务只监听回环地址，不对校园网络开放旧开发服务器。未执行会跨版本破坏适配的audit fix --force。管理端可兼容修复的Axios告警已在迁移说明中记录兼容的1.x安全更新（1.13.2→1.20.0，更新后管理端audit为0）。

Windows云端首轮发现cmd.exe对嵌套命令引号处理不同，已改为校验固定安全参数后直接传入命令；原生java/docker仍保持shell:false。Windows CI检出目录包含空格，以验证真实路径处理。Actions版本已按官方当前发布更新至checkout7.0.1、setup-node7.0.0、setup-java6.0.0；实际结果见Actions。

Windows环境的Path在复制成普通JavaScript对象后需要显式按大小写归一化。为JDK添加bin目录时现保留原Path，并移除重复键，避免npm和系统命令从PATH中丢失；由含空格目录的Windows构建继续验收。

## C-01 物品审核管理端增量验证

2026-09-08 在 Windows 隔离开发环境验证管理端物品审核只读切片和开发组件夹具：审核理由必填；请求进行中禁用提交；请求失败和403保留输入并给出对应提示；409保留理由、触发列表回读且不覆盖本地记录；成功分支仅检查包含 `decision/reason/version` 的暂定请求形状，不修改物品状态。Escape 关闭后焦点返回触发按钮，弹层连续快速开关4次无残留遮罩或滚动锁；375、768及1280像素无页面级横向溢出，浅色、深色与减少动态效果均已检查。Node逻辑测试3项通过，Vite生产构建通过且产物不包含开发夹具路由；继承的分包体积提醒仍存在。

上述成功、403、409均为明确标识的组件夹具分支，不是真实审核接口或数据库证据。A-02 尚未实现审核写接口、版本字段、审核元数据和状态筛选，因此未执行真实ADMIN持久化回读、普通用户服务端拒绝、两管理员并发冲突或数据库验证；D-01仍直发 `AVAILABLE`，B-02仍只读取 `AVAILABLE`。本次不访问或写入成员日常数据库，不把审核闭环或整个C-01标记完成。

## C-01 分类维护前端切片（2026-09-08）

- 管理端生产构建通过；分类表单纯逻辑测试3项通过。当前机器全局 npm 启动脚本缺少 `npm-cli.js`，因此使用同一 Node 24.20.0 和项目已安装的 Vite 8.2.2 入口直接执行，未改依赖或 lockfile。
- 浏览器从真实 `GET /api/categories` 回读6条隔离环境分类；正式新增、编辑、停用/删除保持禁用，没有调用未实现写接口。
- 开发环境夹具实际验证空白/64字符上限、请求失败与403保留输入、409触发列表回读而不覆盖、提交期间按钮禁用；弹层 Escape 返回触发按钮，快速开关4次后无可见遮罩或滚动锁。
- 1280、768、375像素视口无页面横向溢出；浅色和深色均检查。应用内减少动态效果开启后，分类弹层和导航指示器计算过渡时长均为0秒，验证后已还原原偏好。
- 未验证分类写入、刷新持久化、真实403/409引用保护或数据库变更，因为 A-02 分类写 API、版本和引用约束尚未实现；本切片不能标记为完成。微信平台不在本管理端切片范围内，未验证。

## C-01 账号查询与启停管理端切片（2026-09-08）

- 管理端账号表单逻辑测试10项、Vite生产构建及UniApp既有回归26项通过；后端在隔离H2环境通过42项匹配测试和37项API集成测试。生产构建仅保留既有分包体积提醒，没有新增依赖或更改lockfile。
- 浏览器通过真实A-01接口和隔离H2内存数据库验证ADMIN账号查询、角色/状态筛选、分页、安全字段详情、启停审计、停用与恢复启用后的刷新持久化；理由空白被客户端拦截，提交期间禁用重复操作。普通用户访问管理接口由服务端返回403。
- 当前管理员自停用由服务端返回409且保留理由；模拟另一管理员先提交后，旧版本请求返回409，弹层保留理由并回读目标最新状态和版本，没有自动重试或覆盖。账号停用后既有会话返回401，恢复启用后该旧会话仍为401。
- 375、768及1280像素视口无页面级横向溢出；浅色、深色、Escape关闭后的焦点返回、确认弹层连续快速开关4次及减少动态效果均已检查，关闭后无可见遮罩或body滚动锁。
- 本次真实API/数据库浏览器证据仅来自临时H2环境，进程结束后数据已清理；本次未运行MySQL，不能以A-01后端切片既有MySQL证据替代本次前端联调。未执行微信开发者工具或真机，本管理端切片也不代表整个C-01完成。

## C-02 本阶段新增控件统一（2026-09-08）

- 管理端、UniApp H5、微信小程序生产构建通过；当前机器全局 npm 入口缺少 `npm-cli.js`，因此使用同一 Node 24.20.0 与项目锁定的本地 Vite/Uni CLI 入口执行，没有修改依赖或 lockfile。管理端仅保留既有大分包提醒。管理端逻辑回归10项、UniApp回归49项、微信构建连线检查6项通过。
- 管理端与H5控件实验室实际验证按钮/图标按钮、Tab/分段、输入错误、选择/日期、开关、分页、上传入口、菜单/消息/通知、弹窗/抽屉的可见状态。加载期间按钮禁用；本地失败演示明确不请求业务API。快速反向切换分段后指示器回到当前选项。
- 管理端弹窗与抽屉、H5 `LoopSheet` 均检查Escape关闭和焦点返回；H5打开后第一次Tab被圈入抽屉，连续快速开关4次后无可见遮罩、公共锁已释放且关闭层不接收指针。共享遮罩、焦点环、禁用透明度和原生粉色值均从根token或公共平台镜像读取。
- 375、768和1280像素下两端控件页均无页面级横向溢出；管理端与H5浅深色已实际切换。两端减少动态开关令分段/导航/弹层过渡为0秒；H5简化材质开关令公共玻璃层 `backdrop-filter:none` 且使用实色表面。实际交换推荐未登录页在375/1280像素保持实色内容表面且不伪造推荐。
- 微信产物确认H5 DOM滚动锁与简化材质演示被条件编译移除，原生导航、slider、modal颜色镜像共享语义值；本次未运行微信开发者工具或真机，不能用H5替代。未使用账号凭据，因此D-03登录后推荐预览只通过同一 `LoopSheet` 公共实现和构建覆盖，仍待D联调。纯样式没有新增镜像实现测试。

## A-01 第一批增量验证

2026-09-07 在 Windows 工作区执行本人资料与修改密码后端切片验证：内存 H2 与一次性 MySQL 8.4.11 容器均为8项纯匹配测试、9项 Spring/Flyway/Mapper 集成测试通过，共17项且无失败；提交快照的隔离副本另行完成 Maven `verify` 和 Spring Boot 可执行 JAR 重打包。新增用例实际覆盖未登录401、受保护字段400且无篡改、资料写入后通过 API 与数据库回读、旧密码错误无副作用、两个原会话全部失效、旧密码登录失败、新密码登录成功，以及旧密码登录与改密并发时不会留下有效旧密码会话。MySQL 使用随机口令的 `campus_loop_ci_test` 临时库，脚本结束后停止容器；未连接或写入成员日常开发库。

本切片未变更前端，未执行管理端/H5浏览器、微信开发者工具或真机验证；这些结果不能由后端自动化代替。在该次第一批验收时，A-01 的注册、更多个人资料、细粒度权限与账号审计仍未完成。

## A-01 第二批增量验证

2026-09-08 在 Windows 工作区执行管理员用户查询与账号启停后端切片验证：内存 H2 与一次性 MySQL 8.4.11 容器均为8项纯匹配测试、14项 Spring/Flyway/Mapper 集成测试通过，共22项且无失败。新增用例实际覆盖未登录401与普通用户403、关键词/角色/状态分页筛选和安全字段、受保护写字段拒绝、停用后两个旧会话与新登录失效、重新启用不恢复旧令牌、启停审计、历史物品保留、同版本并发写仅一个成功、登录/停用竞态不留下有效会话、当前管理员自我停用拒绝，以及两个管理员交叉停用时保留可用管理入口。MySQL 使用随机口令的 `campus_loop_ci_test` 临时库，V3 迁移和真实 InnoDB 锁语义通过后容器停止；未连接或写入成员日常开发库。

本切片只变更后端与协作文档，未实现或修改管理页面，未执行管理端/H5浏览器、微信开发者工具或真机验证。A-01 的注册、校园准入、更多个人资料及细粒度权限仍未完成；本批不能视为整个 A-01 完成。

## A-01 第三批增量验证

2026-09-08 在 Windows 工作区将注册分支变基到已合入 PR #4/#5/#6 的最新 main 后执行注册准入与注册接口后端切片验证：默认自动化为8项纯匹配测试、27项 Spring/Flyway/Mapper 集成测试，共35项且无失败。新增注册用例实际覆盖默认关闭返回403且无用户记录、显式开发自助模式正常注册并沿用现有登录、密码只存 BCrypt 哈希、服务端固定 `USER/ACTIVE`、非法输入及受保护字段400且不能提权、顺序重名409，以及两个并发相同用户名请求仅一个成功且数据库只保留一条用户；同时保留管理员启停、D-01 与 B-01 回归。

同一组35项测试已在 `scripts/mysql-test.mjs` 启动的随机口令一次性 MySQL 8.4.11 容器通过；使用 `campus_loop_ci_test` 隔离库，验证 Flyway V1–V4 既有迁移、用户名唯一约束和并发结论，完成后容器已停止，未连接或写入成员日常开发库。Flyway 对 MySQL 8.4 仍输出既有的版本支持提醒，不影响本次实测通过。

仓库交付物检查通过：已跟踪文件中没有本地环境、依赖目录、构建产物或上传数据。本切片未变更前端，未执行管理端/H5浏览器、微信开发者工具或真机验证；分支 CI 需在推送及创建 PR 后确认。开发自助注册不等同于校园身份核验，公开准入、邀请码、校园服务、短信和邮件均未实现。A-01 的其他切片仍按路线图独立跟踪，未把整个 A-01 标记完成。

## B-01 独立需求增量验证

2026-09-08：H2与全新一次性MySQL 8.4.11均通过8项匹配、18项集成测试（共26项，新增9项B-01）。另一个临时MySQL容器先应用V1/V2和原有测试夹具，再升级到当次 B-01 分支的V3并通过26项测试，逐行核对旧物品全部字段未改变。两个容器均已自动停止并删除，未连接成员日常库。API通过MockMvc进入真实认证/服务/Mapper，持久化与并发版本冲突实际发生在MySQL；不等同于浏览器或微信联调。

接口、边界及逐层证据见 [B-01 接入说明](b01-independent-demands.md)。B-02匹配与正式交换未接入；该次验收时，A的迁移序号及A/D候选基数确认、其他成员审核和PR CI仍须合并前完成。

## B-01 同步 main 与迁移序号修复

2026-09-08：将 main `1bf1b78` 合入 B-01，保留 A-01 账号启停、D-01 物品浏览/发布恢复及双方测试。解决两个文档冲突，将需求 SQL 原文由暂定 V3 顺延为 V4；逐字核对 main 的 V1–V3 均未改写。已确认旧 B-01 V3 没有需要保留的开发库数据。

Maven clean 后完整构建通过：管理端、H5、微信小程序和后端 verify；H2 为8项匹配与23项集成测试，共31项。全新一次性 MySQL 8.4.11 从空库应用 V1–V4，同样31项通过。另一个独立 MySQL 执行主线 V3→V4 专项升级测试：2名虚构用户及版本、1条状态审计、1件旧物品的全部字段保留，V1–V3迁移历史和校验值不变，需求与关联写入、唯一键和外键生效，再次迁移执行0项。该专项JUnit 1项通过，两个临时容器均自动清理。

本次未连接日常库，未执行浏览器、微信工具或真机交互。仓库文件与差异检查通过；云端 CI 和其他成员审核以 PR #4 最新状态为准，A/D候选基数复核仍待完成。

## B-02 独立需求只读匹配

2026-09-08：`node scripts/run.mjs test`和`node scripts/mysql-test.mjs`各64项通过（15项旧规则回归、19项独立规则、30项集成）。真实MySQL8.4.11验证了有效需求/关联、200/201物品及20000/20001关联上限、身份/版本错误、旧字段兼容，并逐行比较读取前后的物品、需求、关联、交换、占用五表无变化。一次性容器已停止并删除，未访问日常数据库；无新增迁移，既有V1–V4实际应用成功。

实际规则生成的虚构两方75分、三方70分、停用空结果及422样例见[B-02说明](b02-independent-matching.md)。本轮未改前端、未做浏览器或微信交互；云端构建/CI和A/D消费复核以该切片PR为准，不将本地自动化冒充平台联调或团队审核。

## 代码审核问题修复

2026-09-08：修复发布页切换账号时的旧草稿与异步回调串用、LoopButton 微信点击/表单能力缺失、密集匹配结果耗尽内存，以及匹配超限导致整个管理概览失败。发布页身份与分类请求均有代次校验；上传和发布回调绑定发起账号，退出或卸载后失效。匹配器限制200件候选、1000条完整推荐；统计超限使用明确的不可用状态，不伪造零值。

本批从已提交主线独立开发，并无冲突同步到 main `01cdd8b`（包含注册功能）；没有纳入并行 B-02 的未提交代码，没有新增或改写数据库迁移。

- 构建：`node scripts/run.mjs build` 的管理端、H5、微信小程序和后端 verify 通过。同步最新 main 后另行完成完整 Maven verify。
- 前端自动回归：`npm test`（在用户端目录）26项通过，其中发布页21项、按钮5项；真实 Vue setup/SFC 执行，覆盖乱序身份/分类返回、上传/发布晚到、取消后重试、卸载、重新登录恢复、点击/键盘及禁用状态。已接入根 test/build 和 CI。
- 微信产物：`npm run test:mp-build` 6项通过（含5项按钮逻辑），核对生成的 bindtap、form-type、loading/ARIA、`wx://form-field-button` 及登录/发布外层表单连接。未执行微信开发者工具或真机，不将产物检查视作微信运行验证。
- 内存限制：匹配模块独立 `-Xmx128m` JVM 中16项通过，包括100/200件密集候选、200/201候选与1000/1001推荐边界、排序和去重。
- 数据库与接口：最新 main 上 H2 与 `scripts/mysql-test.mjs` 的一次性 MySQL 8.4.11 各46项全部通过（16项匹配、30项集成）。新增接口测试实际确认超限返回422、统计保留基础计数且推荐为null、正常零值，以及交换/占用表计数不变。测试容器自动停止删除，未连接日常库。
- 浏览器：真实 Chrome 20项检查通过，所有API均为浏览器隔离的内存夹具，无真实业务写入。覆盖A/B登录切换、验证期间表单隐藏且无发布、各自草稿恢复及单次正确归属提交；按钮鼠标/Enter/Space、长按、加载禁重入、登录原生表单、管理概览超限与恢复；控件和概览390px无横向溢出，控件减少动态效果，页面JavaScript错误为0。

云端 CI 与其他成员审核以本批修复 PR 的最新状态为准；本地检查不替代成员审核。

## 2026-09-08 A-02 管理员分类维护

新增V6分类状态/排序/版本/名称键与约束，ADMIN维护、物品/需求历史引用保护及停用后的写入校验。根test：用户端32项、后端109项（含12项分类集成）通过；一次性MySQL8.4.11运行V1–V6，109项通过并移除容器。MySQL首轮异常类型断言差异已修正，回读断言与完整重跑通过。管理端6项及构建、用户端H5/微信构建、微信产物6项检查、repository-check及diff检查通过。

浏览器在本地HTTP夹具验证管理目录/历史筛选、发布ACTIVE选项和全停用草稿恢复（H5含390px窄屏），不等同页面连接真实MySQL。未运行微信工具/真机、减少动态效果实机检查或C正式维护表单联调。C/D/B字段与状态影响见 [接入说明](a02-category-maintenance.md) 和 [Issue #20](https://github.com/429res/campus-loop/issues/20)，消费确认待回复。

提交期间PR #18需求页合入main，本批同步并补充按分类ID保持选择、排序变化/停用清空、过期响应忽略及400刷新分类的5项回归；根测试及用户端两种构建重新通过。需求页浏览器HTTP夹具验证400后保留说明/version、清空失效选择通过，不等同真实数据库页面联调。

## A-02 第四批：物品审核（2026-09-08）

H2后端119项、临时MySQL8.4.11后端119项通过（42公共模块+77API/集成），包括8项审核集成及2项DTO；另一个全新MySQL的V6→V7专项升级1项通过，旧状态/内容/版本/占用逐行不变、历史直发标识及默认待审正确。未连接成员日常库。管理端6项、用户端36项自动化与两端三种构建通过；具体命令、错误修复后重跑、浏览器真实后端链路和未联调范围见 [审核交付说明](a02-item-review.md)。本条更新当前实现状态，不将前面的C只读夹具记录算作本批真实审核证据。

## B-03 第一切片：领域验证与交换读取（2026-09-08）

用户端47项、H2后端140项通过；全新临时MySQL8.4.11执行V1–V7及后端140项通过（52公共+88API），新增10领域/2DTO/9集成。真实读取与域核查前后逐行比较六张业务表无写入，清理后基线不变；无日常库或凭据输出。A已确认没有A-03事务，正式POST保持501；MySQL夹具读取和既有A-02并发回归不计为交换创建/幂等/抢占/回滚验收。无浏览器或微信交互，前端构建和云端检查见本PR。完整边界与样例见 [B-03说明](b03-exchange-domain.md)。

## A-03 正式创建与B端口集成（2026-09-08）

基于main c8e9185，在独立feature/a03-exchange-transaction工作树完成；原工作区未提交改动保留。B-03 PR #28的HTTP/应用/领域/读取已实际接通，唯一事务实现和失败样例见[A-03](a03-exchange-transaction.md)。

- `node scripts/run.mjs test`：用户端49通过；后端52公共+93 API/集成通过，6项MySQL专用明确跳过，无失败。
- `CAMPUS_TEST_PORT=3333 node scripts/mysql-test.mjs`：全新MySQL8.4.11、Flyway V1–V8，151项后端全部通过；共用ExchangeDomainIntegrationTest为20项，覆盖真实POST两方/三方、重试、同键冲突、并发唯一赢家、FOR UPDATE等待后重校验、冻结与候选需求竞争、五个写阶段唯一约束回滚、SQLSTATE40001整事务重试上限。推荐GET及清理前后逐行业务数据保持。
- `CAMPUS_TEST_PORT=3333 node scripts/mysql-test.mjs --item-review-upgrade`：独立新空库1项通过，V7→V8保留旧交换字段与占用，新摘要/规则/快照NULL，不伪造需求引用；两个最终测试容器自动删除。
- 仓库交付检查、62个相关文档相对链接和样例JSON通过。没有页面改动；浏览器、微信、成员日常数据库/前端联调未执行。确认/取消/交接/自动超时仍未实现，本次不宣称完整交换闭环或B-03全部完成。

## B-03 第二切片：邀请规则准备与独立验证（2026-09-08）

按发起人明确选择，在A-03未实现时先完成契约与独立验证。已同步main `c8e9185`（B-03.1 PR #28），未修改原工作区的已有改动；实际本人列表/详情继续可读，合法创建及已认证confirm/cancel/handoff保持501、allowedActions为空。

- 自动化：`node scripts/run.mjs test`通过用户端49项和后端159项（69公共+90API），无失败或跳过。本片新增17项纯规则及2项集成边界测试，覆盖2/3人逐人确认、READY取消、幂等事件、隐私/版本/交接拒绝和截止等号前后。顺序推演不代表并发锁验证。
- 真实数据库：`CAMPUS_TEST_PORT=3332 node scripts/mysql-test.mjs`在全新一次性MySQL 8.4.11通过同组159项；V1–V7从空库应用成功，无新增迁移。新增测试读取`UTC_TIMESTAMP(6)`作为纯规则时间输入；真实认证501/401路径前后逐行核对物品、需求、需求物品关联、交换、参与者、占用六表无变化。容器自动停止删除，未连接成员日常库、未输出凭据。
- 仓库与样例：repository-check、git diff检查及JSON样例解析通过。D/C样例区分当前501与未来请求/纯规则推演，不包含假HTTP200。
- 边界：没有正式创建/确认/取消事务、条件释放、取消审计迁移或A-04定时扫描。MySQL回归不证明确认取消竞争、占用归属释放、失败整笔回滚或重启恢复；这些须A-03到位后复用共同服务与MySQL并发测试。未执行浏览器、微信开发者工具或真机交互。构建与CI以本PR最终提交的实际检查为准。

详见[B-03.2接入说明](b03-invitation-rules.md)和[协作事项 #30](https://github.com/429res/campus-loop/issues/30)；仅完成本轮规则准备，不标记完整B-03.2或A-04完成。

## B-03 第二切片：A-03接入后的确认/取消（2026-09-08）

集成A-03 PR #32（18a1e63），仍在独立feature/b03-invitation-rules/PR #31工作；上文“规则准备”是此前未接通时的历史记录，本条记录正式生命周期事务的新增验证。创建继续复用A-03，确认/取消/内部到期共用服务和数据库UTC/事务执行器，交接与扫描未实现。

- `node scripts/run.mjs test`：用户端49项通过；后端180项中171项通过、9项MySQL专用明确跳过（69公共、111API/集成），无失败。新增10项共用交换集成测试，复用原有创建、SQL探针和并发工具；连同之前17项纯规则及2项边界测试验证本片。
- `CAMPUS_TEST_PORT=3332 node scripts/mysql-test.mjs`：全新临时MySQL8.4.11执行V1–V9，180项全部通过。共用ExchangeDomainIntegrationTest为30项，包括两方/三方真实POST创建与独立确认到READY、等待/READY取消、严格身份/JSON/版本、重复取消、终态/交接保护、数据库截止及内部到期。真实两连接证明确认/取消两种先后、重复取消、取消/到期串行；持有物品FOR UPDATE直到跨截止后释放，先到达的确认和取消均409且无写入。
- 取消只释放本交换占用；将一件占用归入其他交换、修改owner/status/version均409，八张业务表逐行不变。确认时间、状态、事件、删除占用和物品恢复各阶段通过真实唯一约束失败注入验证整笔回滚，原参与者/占用/版本/审计不留部分结果。恢复不转移owner，不修改reviewBasis。GET及重放仍无额外业务写入。
- `CAMPUS_TEST_PORT=3332 node scripts/mysql-test.mjs --item-review-upgrade`：另一个全新MySQL的1项升级检查通过。保留原V6/V7/V8检查并增加V8→V9，旧交换全部字段/快照/版本与占用保留，取消三字段为NULL，未伪造历史事件。两个容器均自动停止删除，未接成员日常库，未记录凭据。
- 仓库与差异检查、文档相对链接和虚构API样例结构/物品流向检查通过。初轮发现DTO静态方法与record访问器同名、测试幂等键含大写，均已修正；以上为修正后的完整重跑。既有Flyway版本提示未影响本次迁移和事务结果，未升级依赖。

没有页面改动，未做浏览器、微信开发者工具或真机验证。A-04完整扫描/批次/重启恢复仍待后续，不把内部expire调用算作定时恢复联调；交接、所有权转移和C/D页面接入也未完成。构建/云端CI以PR最终提交检查为准，不自动合并main。

本轮云端首次运行中，180项MySQL测试通过，随后升级检查在首次建连时失败、尚未运行迁移断言。检查MySQL8.4.11镜像入口源码确认初始化临时服务使用socket且跳过TCP；原localhost健康探测可能提前宣告就绪。测试脚本改为TCP探测，仍无凭据参数/输出，等待最终服务启动；修复后本地再次启动全新MySQL，升级检查1项通过且容器已删除；最终提交CI另以PR检查记录，不以原失败批次冒充通过。

## A-04 到期扫描与运行恢复（2026-09-08）

独立feature/a04-exchange-expiry基于B-03.2 6025f27，共用生命周期事务/规则/时钟；没有另建状态机。实际锁序、截止与运维参数见[A-04](a04-exchange-expiry.md)。

- 单元/功能：根`node scripts/run.mjs test`通过，用户端49项；公共规则69项，API/功能119项中107通过、12项MySQL专用跳过，无失败。
- 数据库：`CAMPUS_TEST_PORT=3335 node scripts/mysql-test.mjs`，全新MySQL8.4.11，Flyway V1–V10，188项后端全部通过。共用交换套件38项，新增扫描状态矩阵、有界批次/持久退避、异常记录失败可恢复、四个释放写阶段全回滚、他人/新占用保护、两扫描实例SKIP LOCKED、确认/取消与扫描竞态。
- 运行恢复：上述MySQL套件启动两个独立真实Spring Boot JVM，强制停止旧进程，新进程定时器自动读取数据库待办和已到重试时间的失败记录；Pacific/Honolulu进程时区下仍按数据库UTC到期，各记录仅一条EXPIRED事件，无剩余占用。不是仅重建Java对象或手动调用scan；未模拟数据库服务重启或进程在释放中途崩溃。
- 升级：`CAMPUS_TEST_PORT=3335 node scripts/mysql-test.mjs --item-review-upgrade`独立空库1项通过，V9→V10保留业务状态/截止/元数据和占用。最终临时容器、JVM及运行日志全部清理，未连接成员日常库。
- 仓库交付检查、83处相关文档相对链接通过；无前端改动，浏览器/微信/真机未执行。B交接写入口仍501，已有交出/收到事实会阻止普通超时恢复上架；完整交接联调不在本轮证据内。

## A-05 认证与上传限流（2026-09-08）

本功能分支新增6项限流专项自动化：3项并发/时钟/代理核心测试和3项真实Spring MVC、认证、上传服务集成测试。可控UTC时钟验证固定窗口结束立即恢复；64个并发请求在额度17时恰好17个放行。HTTP测试验证已知账号错误口令与未知账号使用相同401、伪造`X-Forwarded-For`不能改变未信任直连主体、第三次请求返回统一429及`Retry-After`/`X-RateLimit-Reset`，跨域响应显式暴露两个恢复头，窗口后恢复原401语义；改密和两类上传按用户ID跨地址共享额度，健康读取不受限制。测试上传只写操作系统临时目录并在结束后删除。

最终限流代码在隔离H2运行6项，全部通过；`node scripts/mysql-test.mjs --rate-limit`另建全新一次性MySQL 8.4.11、从空库应用Flyway V1–V14并运行同6项，全部通过，随后容器删除。没有连接成员日常库或输出随机口令、账号口令和令牌。

合入最新 `main` 后最终执行 `node scripts/run.mjs test`：用户端53项、公共模块75项及API模块156项通过（其中22项MySQL专用明确跳过）。合入前曾执行完整MySQL 154项，其中152项通过、2项既有交换套件未通过：Windows恢复测试删除仍被子进程占用的临时日志时报错，推荐元数据用例期望9而得到0；限流6项在该次运行中仍全部通过。合并后没有重跑完整MySQL套件，不能将其标为成功；已在最终合并结果上另建全新MySQL专项重跑，确认限流6项通过。本PR不修改这两条交换路径。

验证只覆盖单个Spring Boot进程，不替代多实例共享计数、生产代理/防火墙、云网关、公网压力、浏览器、微信开发者工具或真机验证。无数据库迁移、前端或外部服务变更；云端CI以PR最终提交为准。

## A-05 备份恢复演练与部署配置（2026-09-08）

Windows 工作区实际运行 `node scripts/run.mjs backup-restore-test` 通过。脚本使用随机凭据、系统临时目录、动态端口和两个一次性 MySQL 8.4.11 容器，不读取根 `.env`；源库停止应用写入后导出数据库并复制公开/私有上传目录，新库导入后由应用完成回读。虚构物品 #1、需求 #1、履历及其引用一致，公开图和鉴权私有证据分别逐字节 SHA-256 一致；Flyway 14条成功、最新V14、失败0。SQL、上传副本和容器均已清理，未连接3308日常库。

首次运行因旧分支 `target` 残留造成打包内重复V8迁移并安全失败，脚本改为 `clean package` 后完整重跑通过；另一次断言按实际 `offeredItems[].itemId` 契约修正后重跑通过。详细重复步骤、部署变量和反向代理边界见 [A-05说明](a05-backup-restore-deployment.md)。macOS、云数据库、远程部署、真实反向代理、微信工具/真机与恢复性能未验证。
